import meta from "./how-to-guides/_meta.json"

# How-To guides

{Object.entries(meta).map(([group, page]) => {
const meta = require(`./how-to-guides/${group}/_meta.json`)
const entries = Object.entries(meta)
if (entries.length === 0) {
return (

<div>
  <h2>{page}</h2>
  <ul>
    <li>
      <a href={`./how-to-guides/${group}`}>{page}</a>
    </li>
  </ul>
</div>

)
}
return (

<div>
  <h2>{page}</h2>
  <ul>
    {entries.map(([key, item]) => (
      <li>
        <a href={`./how-to-guides/${group}/${key}`}>{item}</a>
      </li>
    ))}
  </ul>
</div>
) })}
