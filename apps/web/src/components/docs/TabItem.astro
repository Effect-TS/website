---
interface Props {
  label: string
  icon?: string
}

const { label } = Astro.props
---

<div class="docs-tab-panel" data-label={label}>
  <p class="docs-tab-fallback-label font-semibold">{label}</p>
  <slot />
</div>
