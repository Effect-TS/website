import { Card, Cards } from 'nextra-theme-docs'
import meta from "./faq/_meta.json"

export const prefix = "faq"

# FAQ

The following are some frequently asked questions about Effect.

<Cards>
  {Object.entries(meta).map(([key, page]) => (
    <Card key={key} title={page} href={`/docs/${prefix}/${key}`} />
  ))}
</Cards>
