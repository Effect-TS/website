---
import { YouTube } from "@astro-community/astro-embed-youtube"

interface Props {
  readonly id: string
  readonly title?: string | undefined
}

const { id, title } = Astro.props
---

<YouTube id={id} title={title} class="min-w-full" />
