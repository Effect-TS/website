---
title: Handle failures without guessing
description: Turn untyped Promise rejections into explicit error decisions, then add bounded retries, timeouts, and cancellation.
sidebar:
  order: 1
---

import {
  AgentPrompt,
  Aside,
  TutorialCheckpoint,
} from "@astrojs/starlight/components"

TypeScript describes the value produced by a `Promise`, but not the reasons why
it may reject. Once provider calls, timeouts, and retries meet in one `catch`,
the backend has to reconstruct what happened before it can decide what to do.

In this tutorial, we will start with a familiar `node:http` endpoint and make
its error handling progressively more precise. The finished `POST /orders`
endpoint will have these observable behaviors:

| Provider behavior                           | Decision                          | HTTP result    |
| ------------------------------------------- | --------------------------------- | -------------- |
| Payment succeeds                            | Accept the order                  | `201`          |
| Payment is declined                         | Do not retry                      | `422`          |
| Provider is temporarily unavailable         | Retry at most twice               | `201` or `503` |
| Provider rejects with an undocumented error | Do not guess                      | `502`          |
| Provider takes too long                     | Interrupt and retry at most twice | `504`          |
| Client disconnects                          | Interrupt the complete operation  | No response    |

We will keep the HTTP server in ordinary TypeScript. Effect will enter only
where it solves a problem we have already observed.

Starting from a conventional implementation gives every change a visible
reason. The initial server is not deliberately broken: it handles the happy
path using tools familiar to a TypeScript backend developer. We will introduce
each Effect API only after encountering the limitation it addresses.

<Aside type="tip" title="Using a coding agent?">
  Each step ends with a checkpoint: a short verification that the code behaves
  as expected before you continue. Every checkpoint includes a button that
  copies an agent-ready prompt. Read the step first, then either follow its
  instructions yourself or let your coding agent perform exactly the same
  changes and checks.
</Aside>

## 1. Start from familiar TypeScript

To perform the setup manually, create an empty project. Otherwise, skip to the
first checkpoint card below. Its prompt will create the complete starter
project, start the server, and verify the first request with your coding agent.

```sh title="Create the project"
mkdir handle-failures-without-guessing
cd handle-failures-without-guessing
npm init -y
npm pkg set type=module
npm install --save-dev @types/node@22
```

Create `server.ts` with the code below. The first part is the application. The
fixture at the bottom behaves like a small Promise-based payment SDK. We will
not modify the fixture during the tutorial.

```ts title="server.ts" collapse={45-87}
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

    const authorization = await authorizePayment(input.paymentToken)

    sendJson(201, {
      orderId: `order-${input.customerId}`,
      status: "accepted",
      paymentProvider: authorization.provider,
    })
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener(
    "abort",
    () => console.log("primary-pay authorization aborted"),
    {
      once: true,
    },
  )
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

Start the server with `--watch` so Node.js reloads it automatically after each
edit. Keep this terminal running:

```sh title="Keep running throughout the tutorial"
node --watch server.ts
```

Open a second terminal pane to verify the happy path:

```sh title="Send an approved payment"
curl -i -sS http://localhost:3000/orders \
  -H 'content-type: application/json' \
  -d '{"customerId":"customer-approved","paymentToken":"tok_approved"}'
```

The endpoint responds with:

```text title="Expected response"
HTTP/1.1 201 Created
...

{"orderId":"order-customer-approved","status":"accepted","paymentProvider":"primary-pay"}
```

We have a working HTTP endpoint before introducing any Effect code.

<TutorialCheckpoint
  number={1}
  total={11}
  title="The starter endpoint works"
>
  The project is running and an approved payment returns `201`. Continue only
  after you can reproduce the response shown above.

  <AgentPrompt label="AI prompt: set up the project">

````text
Set up the starter project for the Effect tutorial.

Rules:
- Treat the current workspace root as the parent directory.
- Work only in `./handle-failures-without-guessing`.
- If that directory already exists, stop without changing anything and explain that the setup requires a new directory.
- Do not add files, dependencies, or configuration not requested below.

Create `./handle-failures-without-guessing`, then run these commands inside it:

```sh
npm init -y
npm pkg set type=module
npm install --save-dev @types/node@22
```

Create `server.ts` with exactly this content:

```ts
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

    const authorization = await authorizePayment(input.paymentToken)

    sendJson(201, {
      orderId: `order-${input.customerId}`,
      status: "accepted",
      paymentProvider: authorization.provider,
    })
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

Start `node --watch server.ts` from the tutorial directory in a persistent terminal and wait for `Listening on http://localhost:3000`. Leave the server active.

Send a POST request to `http://localhost:3000/orders` with `content-type: application/json` and this body:

```json
{"customerId":"customer-approved","paymentToken":"tok_approved"}
```

Verify status `201` and this response body:

```json
{"orderId":"order-customer-approved","status":"accepted","paymentProvider":"primary-pay"}
```

When finished, reply only that the starter project is running and the approved request succeeded. If any requested action fails, report the failure without attempting an alternative setup or correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 2. See what goes wrong

Send two failures that should require different decisions:

```sh title="Compare two provider failures"
# Final business decision
curl -i -sS http://localhost:3000/orders \
  -H 'content-type: application/json' \
  -d '{"customerId":"customer-declined","paymentToken":"tok_declined"}'

# Temporary outage
curl -i -sS http://localhost:3000/orders \
  -H 'content-type: application/json' \
  -d '{"customerId":"customer-flaky","paymentToken":"tok_flaky"}'
```

Both requests produce the same response:

```text title="Expected response"
HTTP/1.1 500 Internal Server Error
...

{"error":"InternalServerError"}
```

The outcomes require opposite decisions:

- a payment decline is final and **must not be retried**
- a temporary outage may recover and **can be retried**

The `catch` receives an `unknown` value and the return type of
`authorizePayment` describes only its successful value. TypeScript does not
carry either failure through the program.

Retrying every rejection could charge a declined card repeatedly. Retrying
nothing turns a temporary outage into an unnecessary failed order. We need to
preserve enough information to decide.

<TutorialCheckpoint
  number={2}
  total={11}
  title="The failure ambiguity is visible"
>
  A final decline and a temporary outage both collapse to the same `500`
  response. Confirm both outcomes before continuing.

  <AgentPrompt label="AI prompt: reproduce the failures">

```text
Reproduce the baseline behavior of the tutorial project.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that `package.json` and `server.ts` exist there. Otherwise stop without changing anything.
- Do not edit any file.
- If `http://localhost:3000/orders` refuses the connection, start `node --watch server.ts` from the tutorial directory in a persistent terminal, wait for `Listening on http://localhost:3000`, and retry. Leave that server active.
- If the server returns any HTTP response, do not restart it merely because a result differs from the expectations below.
- Do not fix a failed verification.

Send these two POST requests with `content-type: application/json`:

1. `{"customerId":"customer-declined","paymentToken":"tok_declined"}`
2. `{"customerId":"customer-flaky","paymentToken":"tok_flaky"}`

Verify these results:

1. The declined request returns status `500` and `{"error":"InternalServerError"}`.
2. The temporary outage returns status `500` and `{"error":"InternalServerError"}`.

Reply with a concise pass or fail result for each request. On failure, report only the observed difference. Do not propose or apply a correction.
```

  </AgentPrompt>
</TutorialCheckpoint>

## 3. Bring the Promise into Effect

Keep the server running. In the second pane, add the dependency:

```sh title="Install Effect"
npm install effect
```

Make the following changes to `server.ts`. Green lines are additions; the red
line is replaced by the green line immediately below it.

```ts title="server.ts" collapse={47-90} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
+import { Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

+const authorizePaymentEffect = (token: string) =>
+  Effect.tryPromise((signal) => authorizePayment(token, { signal }))
+
const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

-    const authorization = await authorizePayment(input.paymentToken)
+    const authorization = await Effect.runPromise(
+      authorizePaymentEffect(input.paymentToken),
+    )

    sendJson(201, {
      orderId: `order-${input.customerId}`,
      status: "accepted",
      paymentProvider: authorization.provider,
    })
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

`Effect.tryPromise` receives a function instead of an already-running Promise.
The function also receives an `AbortSignal`, which is passed directly to the
provider SDK.

An `Effect<Success, Error>` describes an operation that may produce `Success`
or fail with `Error`. It does not start until it is executed. Here,
`Effect.runPromise` is the boundary that executes it inside our existing
`async` handler.

Wait for the server to reload, then send the approved request again. It still
returns `201`. The decline and outage still return `500`. This first change only
establishes the boundary between Promise code and Effect code.

<TutorialCheckpoint
  number={3}
  total={11}
  title="The Promise now runs through Effect"
>
  The approved request still returns `201`, but the provider operation now
  enters Effect through `Effect.tryPromise` and runs at the existing async
  boundary.

  <AgentPrompt label="AI prompt: bring the Promise into Effect">

````text
Bring the provider Promise into Effect without changing the endpoint behavior.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts` and the npm dependency files changed by `npm install effect`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- After applying the change, verify the behavior but do not fix a failed verification.

From the tutorial directory, run `npm install effect`, then apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
 import { createServer } from "node:http"
 import { setTimeout as sleep } from "node:timers/promises"
+import { Effect } from "effect"
@@
 interface PlaceOrderRequest {
   readonly customerId: string
   readonly paymentToken: string
 }
+
+const authorizePaymentEffect = (token: string) =>
+  Effect.tryPromise((signal) => authorizePayment(token, { signal }))

 const server = createServer(async (request, response) => {
@@
-    const authorization = await authorizePayment(input.paymentToken)
+    const authorization = await Effect.runPromise(
+      authorizePaymentEffect(input.paymentToken),
+    )
*** End Patch
```

If the approved request refuses the connection, start `node --watch server.ts` in a persistent terminal, wait for the listening message, and retry. Leave the server active. If the server returns any HTTP response, do not restart it merely because the response is wrong.

Verify that the approved request still returns status `201` with this body:

```json
{"orderId":"order-customer-approved","status":"accepted","paymentProvider":"primary-pay"}
```

Reply only that the checkpoint succeeded, or report the patch mismatch or observed verification difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

After every change below, wait for `node --watch` to reload the server before
trying the next request. Each reload also resets the controlled fixture. Every
modification is shown in a complete `server.ts`: green lines are added, red lines
are removed, and the copy button copies the complete final file without the red
lines.

## 4. Compose the successful result

We now move the successful HTTP result into the Effect program without changing
its behavior. Make the following changes:

```ts title="server.ts" collapse={70-113} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
import { Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

+interface HttpResult {
+  readonly status: number
+  readonly body: unknown
+}
+
+const httpResult = (status: number, body: unknown): HttpResult => ({
+  status,
+  body,
+})
+
const authorizePaymentEffect = (token: string) =>
  Effect.tryPromise((signal) => authorizePayment(token, { signal }))

+const placeOrder = (input: PlaceOrderRequest) => {
+  return authorizePaymentEffect(input.paymentToken).pipe(
+    Effect.map((authorization) =>
+      httpResult(201, {
+        orderId: `order-${input.customerId}`,
+        status: "accepted",
+        paymentProvider: authorization.provider,
+      }),
+    ),
+  )
+}
+
const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

-    const authorization = await Effect.runPromise(
-      authorizePaymentEffect(input.paymentToken),
-    )
-
-    sendJson(201, {
-      orderId: `order-${input.customerId}`,
-      status: "accepted",
-      paymentProvider: authorization.provider,
-    })
+    const result = await Effect.runPromise(placeOrder(input))
+    sendJson(result.status, result.body)
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

`pipe` passes the operation through transformations from top to bottom.
`Effect.map` changes only its success value. It does not catch or alter a
failure.

Wait for the server to reload and send the approved request. The response
remains `201`, confirming that composing the successful result did not change
the endpoint.

<TutorialCheckpoint
  number={4}
  total={11}
  title="Effect preserves the successful behavior"
>
  The successful authorization now becomes an `HttpResult` inside the Effect
  program. The approved request still returns `201`.

  <AgentPrompt label="AI prompt: move success into Effect">

````text
Move the successful HTTP result into the Effect program without changing its behavior.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- After applying the change, verify the behavior but do not fix a failed verification.

Apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
 interface PlaceOrderRequest {
   readonly customerId: string
   readonly paymentToken: string
 }

+interface HttpResult {
+  readonly status: number
+  readonly body: unknown
+}
+
+const httpResult = (status: number, body: unknown): HttpResult => ({
+  status,
+  body,
+})
+
 const authorizePaymentEffect = (token: string) =>
   Effect.tryPromise((signal) => authorizePayment(token, { signal }))
+
+const placeOrder = (input: PlaceOrderRequest) => {
+  return authorizePaymentEffect(input.paymentToken).pipe(
+    Effect.map((authorization) =>
+      httpResult(201, {
+        orderId: `order-${input.customerId}`,
+        status: "accepted",
+        paymentProvider: authorization.provider,
+      }),
+    ),
+  )
+}
@@
-    const authorization = await Effect.runPromise(
-      authorizePaymentEffect(input.paymentToken),
-    )
-
-    sendJson(201, {
-      orderId: `order-${input.customerId}`,
-      status: "accepted",
-      paymentProvider: authorization.provider,
-    })
+    const result = await Effect.runPromise(placeOrder(input))
+    sendJson(result.status, result.body)
*** End Patch
```

If the approved request refuses the connection, start `node --watch server.ts` in a persistent terminal, wait for the listening message, and retry. Leave the server active. If the server returns any HTTP response, do not restart it merely because the response is wrong.

Verify that the approved request still returns status `201` with this body:

```json
{"orderId":"order-customer-approved","status":"accepted","paymentProvider":"primary-pay"}
```

Reply only that the checkpoint succeeded, or report the patch mismatch or observed verification difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 5. Give an unknown provider failure a safe name

The Promise may reject with any value. Our safest starting point is to give an
unrecognized provider rejection its own name, not pretend that it is a decline
or an outage. Make the following changes:

```ts title="server.ts" collapse={58-127} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
-import { Effect } from "effect"
+import { Data, Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

interface HttpResult {
  readonly status: number
  readonly body: unknown
}

const httpResult = (status: number, body: unknown): HttpResult => ({
  status,
  body,
})

+class UnexpectedPaymentProviderError extends Data.TaggedError(
+  "UnexpectedPaymentProviderError",
+)<{
+  readonly cause: unknown
+}> {}
+
+const toPaymentError = (cause: unknown) =>
+  new UnexpectedPaymentProviderError({ cause })
+
const authorizePaymentEffect = (token: string) =>
-  Effect.tryPromise((signal) => authorizePayment(token, { signal }))
+  Effect.tryPromise({
+    try: (signal) => authorizePayment(token, { signal }),
+    catch: toPaymentError,
+  })

const placeOrder = (input: PlaceOrderRequest) => {
  return authorizePaymentEffect(input.paymentToken).pipe(
    Effect.map((authorization) =>
      httpResult(201, {
        orderId: `order-${input.customerId}`,
        status: "accepted",
        paymentProvider: authorization.provider,
      }),
    ),
+    Effect.catchTags({
+      UnexpectedPaymentProviderError: (error) =>
+        Effect.sync(() => {
+          console.error("Unexpected payment provider failure", error.cause)
+          return httpResult(502, {
+            error: error._tag,
+          })
+        }),
+    }),
  )
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

    const result = await Effect.runPromise(placeOrder(input))
    sendJson(result.status, result.body)
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

The Promise rejection enters `catch` as `unknown`. The adapter turns it into a
value whose `_tag` and fields are known to TypeScript. The error channel is now
`UnexpectedPaymentProviderError` instead of a generic unknown error.

Wait for the server to reload, then send the undocumented provider failure:

```sh title="Send an undocumented failure"
curl -i -sS http://localhost:3000/orders \
  -H 'content-type: application/json' \
  -d '{"customerId":"customer-unexpected","paymentToken":"tok_unexpected"}'
```

```text title="Expected response"
HTTP/1.1 502 Bad Gateway
...

{"error":"UnexpectedPaymentProviderError"}
```

Notice that the server logs the original cause but does not expose its message
to the client. At this stage, the documented SDK errors also fall back to `502`.
We will recognize them individually next.

<TutorialCheckpoint
  number={5}
  total={11}
  title="Unknown failures have a safe name"
>
  An undocumented provider rejection returns `502`. Its original cause remains
  in the server log instead of leaking to the client.

  <AgentPrompt label="AI prompt: handle unknown failures">

````text
Give every unrecognized provider rejection a safe application-level name.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- After applying the change, verify the behavior but do not fix a failed verification.

Apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
-import { Effect } from "effect"
+import { Data, Effect } from "effect"
@@
 const httpResult = (status: number, body: unknown): HttpResult => ({
   status,
   body,
 })

+class UnexpectedPaymentProviderError extends Data.TaggedError(
+  "UnexpectedPaymentProviderError",
+)<{
+  readonly cause: unknown
+}> {}
+
+const toPaymentError = (cause: unknown) =>
+  new UnexpectedPaymentProviderError({ cause })
+
 const authorizePaymentEffect = (token: string) =>
-  Effect.tryPromise((signal) => authorizePayment(token, { signal }))
+  Effect.tryPromise({
+    try: (signal) => authorizePayment(token, { signal }),
+    catch: toPaymentError,
+  })
@@
       }),
     ),
+    Effect.catchTags({
+      UnexpectedPaymentProviderError: (error) =>
+        Effect.sync(() => {
+          console.error("Unexpected payment provider failure", error.cause)
+          return httpResult(502, {
+            error: error._tag,
+          })
+        }),
+    }),
   )
 }
*** End Patch
```

If the request refuses the connection, start `node --watch server.ts` in a persistent terminal, wait for the listening message, and retry. Leave the server active. If the server returns any HTTP response, do not restart it merely because the response is wrong.

Send `tok_unexpected` and verify status `502` with body `{"error":"UnexpectedPaymentProviderError"}`. Confirm that the server logs the original cause without exposing its message to the client.

Reply only that the checkpoint succeeded, or report the patch mismatch or observed verification difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 6. Preserve a business decision

A declined payment is a business decision that must remain distinct from an
unexpected provider failure. Make the following changes:

```ts title="server.ts" collapse={73-142} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
import { Data, Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

interface HttpResult {
  readonly status: number
  readonly body: unknown
}

const httpResult = (status: number, body: unknown): HttpResult => ({
  status,
  body,
})

class UnexpectedPaymentProviderError extends Data.TaggedError(
  "UnexpectedPaymentProviderError",
)<{
  readonly cause: unknown
}> {}

-const toPaymentError = (cause: unknown) =>
-  new UnexpectedPaymentProviderError({ cause })
+class PaymentDeclined extends Data.TaggedError("PaymentDeclined")<{
+  readonly reason: string
+}> {}

+const toPaymentError = (cause: unknown) => {
+  if (cause instanceof PaymentDeclinedSdkError) {
+    return new PaymentDeclined({ reason: cause.reason })
+  }
+  return new UnexpectedPaymentProviderError({ cause })
+}
+
const authorizePaymentEffect = (token: string) =>
  Effect.tryPromise({
    try: (signal) => authorizePayment(token, { signal }),
    catch: toPaymentError,
  })

const placeOrder = (input: PlaceOrderRequest) => {
  return authorizePaymentEffect(input.paymentToken).pipe(
    Effect.map((authorization) =>
      httpResult(201, {
        orderId: `order-${input.customerId}`,
        status: "accepted",
        paymentProvider: authorization.provider,
      }),
    ),
    Effect.catchTags({
+      PaymentDeclined: (error) =>
+        Effect.succeed(
+          httpResult(422, {
+            error: error._tag,
+            reason: error.reason,
+          }),
+        ),
      UnexpectedPaymentProviderError: (error) =>
        Effect.sync(() => {
          console.error("Unexpected payment provider failure", error.cause)
          return httpResult(502, {
            error: error._tag,
          })
        }),
    }),
  )
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

    const result = await Effect.runPromise(placeOrder(input))
    sendJson(result.status, result.body)
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

Wait for the server to reload, then send `tok_declined` again:

```text title="Expected response"
HTTP/1.1 422 Unprocessable Entity
...

{"error":"PaymentDeclined","reason":"card declined"}
```

The server log contains one authorization attempt. We have preserved a business
decision from the Promise boundary to the HTTP boundary without throwing it
back into the outer `catch`.

<TutorialCheckpoint
  number={6}
  total={11}
  title="A decline remains a business decision"
>
  A declined payment returns `422` after one attempt. It no longer shares the
  response used for an unknown provider failure.

  <AgentPrompt label="AI prompt: handle payment declines">

````text
Preserve a declined payment as an explicit business decision.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- After applying the change, verify the behavior but do not fix a failed verification.

Apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
 class UnexpectedPaymentProviderError extends Data.TaggedError(
   "UnexpectedPaymentProviderError",
 )<{
   readonly cause: unknown
 }> {}

-const toPaymentError = (cause: unknown) =>
-  new UnexpectedPaymentProviderError({ cause })
+class PaymentDeclined extends Data.TaggedError("PaymentDeclined")<{
+  readonly reason: string
+}> {}
+
+const toPaymentError = (cause: unknown) => {
+  if (cause instanceof PaymentDeclinedSdkError) {
+    return new PaymentDeclined({ reason: cause.reason })
+  }
+  return new UnexpectedPaymentProviderError({ cause })
+}
@@
     ),
     Effect.catchTags({
+      PaymentDeclined: (error) =>
+        Effect.succeed(
+          httpResult(422, {
+            error: error._tag,
+            reason: error.reason,
+          }),
+        ),
       UnexpectedPaymentProviderError: (error) =>
*** End Patch
```

If the request refuses the connection, start `node --watch server.ts` in a persistent terminal, wait for the listening message, and retry. Leave the server active. If the server returns any HTTP response, do not restart it merely because the response is wrong.

Send `tok_declined` and verify status `422`, body `{"error":"PaymentDeclined","reason":"card declined"}`, and exactly one authorization attempt.

Reply only that the checkpoint succeeded, or report the patch mismatch or observed verification difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 7. Preserve a temporary outage

A temporary outage is the second documented provider failure. Make the
following changes:

```ts title="server.ts" collapse={85-154} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
import { Data, Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

interface HttpResult {
  readonly status: number
  readonly body: unknown
}

const httpResult = (status: number, body: unknown): HttpResult => ({
  status,
  body,
})

class UnexpectedPaymentProviderError extends Data.TaggedError(
  "UnexpectedPaymentProviderError",
)<{
  readonly cause: unknown
}> {}

class PaymentDeclined extends Data.TaggedError("PaymentDeclined")<{
  readonly reason: string
}> {}

+class ProviderUnavailable extends Data.TaggedError("ProviderUnavailable")<{
+  readonly provider: string
+}> {}
+
const toPaymentError = (cause: unknown) => {
  if (cause instanceof PaymentDeclinedSdkError) {
    return new PaymentDeclined({ reason: cause.reason })
  }
+  if (cause instanceof ProviderUnavailableSdkError) {
+    return new ProviderUnavailable({ provider: cause.provider })
+  }
  return new UnexpectedPaymentProviderError({ cause })
}

const authorizePaymentEffect = (token: string) =>
  Effect.tryPromise({
    try: (signal) => authorizePayment(token, { signal }),
    catch: toPaymentError,
  })

const placeOrder = (input: PlaceOrderRequest) => {
  return authorizePaymentEffect(input.paymentToken).pipe(
    Effect.map((authorization) =>
      httpResult(201, {
        orderId: `order-${input.customerId}`,
        status: "accepted",
        paymentProvider: authorization.provider,
      }),
    ),
    Effect.catchTags({
      PaymentDeclined: (error) =>
        Effect.succeed(
          httpResult(422, {
            error: error._tag,
            reason: error.reason,
          }),
        ),
+      ProviderUnavailable: (error) =>
+        Effect.succeed(
+          httpResult(503, {
+            error: error._tag,
+            provider: error.provider,
+          }),
+        ),
      UnexpectedPaymentProviderError: (error) =>
        Effect.sync(() => {
          console.error("Unexpected payment provider failure", error.cause)
          return httpResult(502, {
            error: error._tag,
          })
        }),
    }),
  )
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

    const result = await Effect.runPromise(placeOrder(input))
    sendJson(result.status, result.body)
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

Wait for the server to reload, then send `tok_flaky`. The provider is now
reported honestly after one attempt:

```text title="Expected response"
HTTP/1.1 503 Service Unavailable
...

{"error":"ProviderUnavailable","provider":"primary-pay"}
```

The error channel now distinguishes a final business decision, a temporary
availability problem, and an undocumented provider failure. We can finally add
a retry without guessing.

<TutorialCheckpoint
  number={7}
  total={11}
  title="A temporary outage remains recoverable"
>
  A temporary provider outage returns `503` after one attempt. The error channel
  now distinguishes all three failure decisions.

  <AgentPrompt label="AI prompt: handle temporary outages">

````text
Preserve a temporary provider outage as an explicit availability failure.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- After applying the change, verify the behavior but do not fix a failed verification.

Apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
 class PaymentDeclined extends Data.TaggedError("PaymentDeclined")<{
   readonly reason: string
 }> {}

+class ProviderUnavailable extends Data.TaggedError("ProviderUnavailable")<{
+  readonly provider: string
+}> {}
+
 const toPaymentError = (cause: unknown) => {
   if (cause instanceof PaymentDeclinedSdkError) {
     return new PaymentDeclined({ reason: cause.reason })
   }
+  if (cause instanceof ProviderUnavailableSdkError) {
+    return new ProviderUnavailable({ provider: cause.provider })
+  }
   return new UnexpectedPaymentProviderError({ cause })
 }
@@
           }),
         ),
+      ProviderUnavailable: (error) =>
+        Effect.succeed(
+          httpResult(503, {
+            error: error._tag,
+            provider: error.provider,
+          }),
+        ),
       UnexpectedPaymentProviderError: (error) =>
*** End Patch
```

If the request refuses the connection, start `node --watch server.ts` in a persistent terminal, wait for the listening message, and retry. Leave the server active. If the server returns any HTTP response, do not restart it merely because the response is wrong.

Send `tok_flaky` and verify status `503`, body `{"error":"ProviderUnavailable","provider":"primary-pay"}`, and exactly one authorization attempt.

Reply only that the checkpoint succeeded, or report the patch mismatch or observed verification difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 8. Retry only the failure that may recover

Add a bounded retry immediately after `authorizePaymentEffect`. Its predicate
allows only the temporary provider outage to retry:

```ts title="server.ts" collapse={5-48,89-158} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
import { Data, Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

interface HttpResult {
  readonly status: number
  readonly body: unknown
}

const httpResult = (status: number, body: unknown): HttpResult => ({
  status,
  body,
})

class UnexpectedPaymentProviderError extends Data.TaggedError(
  "UnexpectedPaymentProviderError",
)<{
  readonly cause: unknown
}> {}

class PaymentDeclined extends Data.TaggedError("PaymentDeclined")<{
  readonly reason: string
}> {}

class ProviderUnavailable extends Data.TaggedError("ProviderUnavailable")<{
  readonly provider: string
}> {}

const toPaymentError = (cause: unknown) => {
  if (cause instanceof PaymentDeclinedSdkError) {
    return new PaymentDeclined({ reason: cause.reason })
  }
  if (cause instanceof ProviderUnavailableSdkError) {
    return new ProviderUnavailable({ provider: cause.provider })
  }
  return new UnexpectedPaymentProviderError({ cause })
}

const authorizePaymentEffect = (token: string) =>
  Effect.tryPromise({
    try: (signal) => authorizePayment(token, { signal }),
    catch: toPaymentError,
  })

const placeOrder = (input: PlaceOrderRequest) => {
  return authorizePaymentEffect(input.paymentToken).pipe(
+    Effect.retry({
+      times: 2,
+      while: (error) => error._tag === "ProviderUnavailable",
+    }),
    Effect.map((authorization) =>
      httpResult(201, {
        orderId: `order-${input.customerId}`,
        status: "accepted",
        paymentProvider: authorization.provider,
      }),
    ),
    Effect.catchTags({
      PaymentDeclined: (error) =>
        Effect.succeed(
          httpResult(422, {
            error: error._tag,
            reason: error.reason,
          }),
        ),
      ProviderUnavailable: (error) =>
        Effect.succeed(
          httpResult(503, {
            error: error._tag,
            provider: error.provider,
          }),
        ),
      UnexpectedPaymentProviderError: (error) =>
        Effect.sync(() => {
          console.error("Unexpected payment provider failure", error.cause)
          return httpResult(502, {
            error: error._tag,
          })
        }),
    }),
  )
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

    const result = await Effect.runPromise(placeOrder(input))
    sendJson(result.status, result.body)
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

Wait for the server to reload, then send `tok_flaky` again:

```text title="Expected response"
HTTP/1.1 201 Created
...

{"orderId":"order-customer-flaky","status":"accepted","paymentProvider":"primary-pay"}
```

The server logs one initial attempt and two retries:

```text title="Expected server log"
primary-pay authorization attempt 1
primary-pay authorization attempt 2
primary-pay authorization attempt 3
```

Send `tok_declined` once more. It still returns `422`, and the log still contains
only one attempt. The retry predicate receives the typed error and permits only
`ProviderUnavailable`.

This tutorial keeps the timing policy simple. A production policy can also add
spacing, exponential backoff, and jitter with
[`Schedule`](/docs/v4/scheduling/introduction), without changing the error
classification we built here.

<TutorialCheckpoint
  number={8}
  total={11}
  title="Only recoverable failures are retried"
>
  A temporary outage succeeds after three bounded attempts. A declined payment
  still returns `422` after one attempt, so retry does not override a business
  decision.

  <AgentPrompt label="AI prompt: retry recoverable failures">

````text
Add a bounded retry that applies only to temporary provider outages.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- After applying the change, verify the behavior but do not fix a failed verification.

Apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
 const placeOrder = (input: PlaceOrderRequest) => {
   return authorizePaymentEffect(input.paymentToken).pipe(
+    Effect.retry({
+      times: 2,
+      while: (error) => error._tag === "ProviderUnavailable",
+    }),
     Effect.map((authorization) =>
*** End Patch
```

If the server refuses a connection, start `node --watch server.ts` in a persistent terminal, wait for the listening message, and retry. Leave the server active. If the server returns any HTTP response, do not restart it merely because the response is wrong.

Verify both decisions:

1. Send `tok_flaky`. Expect status `201`, an accepted order body, and exactly three authorization attempts.
2. Send `tok_declined`. Expect status `422`, a `PaymentDeclined` body, and exactly one authorization attempt.

Reply with a concise pass or fail result for each decision. On failure, report only the patch mismatch or observed difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 9. Bound every slow attempt

Before adding a timeout, observe the current behavior. Send the slow token:

```sh title="Send a slow payment"
curl -i -sS http://localhost:3000/orders \
  -H 'content-type: application/json' \
  -d '{"customerId":"customer-slow","paymentToken":"tok_slow"}'
```

The request waits for about two seconds and then returns `201`. The provider is
allowed to control how long our endpoint waits.

Add a typed timeout around each provider attempt and handle it as an HTTP
result:

```ts title="server.ts" collapse={104-173} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
import { Data, Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

interface HttpResult {
  readonly status: number
  readonly body: unknown
}

const httpResult = (status: number, body: unknown): HttpResult => ({
  status,
  body,
})

class UnexpectedPaymentProviderError extends Data.TaggedError(
  "UnexpectedPaymentProviderError",
)<{
  readonly cause: unknown
}> {}

class PaymentDeclined extends Data.TaggedError("PaymentDeclined")<{
  readonly reason: string
}> {}

class ProviderUnavailable extends Data.TaggedError("ProviderUnavailable")<{
  readonly provider: string
}> {}

+class RequestTimedOut extends Data.TaggedError("RequestTimedOut")<{
+  readonly provider: string
+}> {}
+
const toPaymentError = (cause: unknown) => {
  if (cause instanceof PaymentDeclinedSdkError) {
    return new PaymentDeclined({ reason: cause.reason })
  }
  if (cause instanceof ProviderUnavailableSdkError) {
    return new ProviderUnavailable({ provider: cause.provider })
  }
  return new UnexpectedPaymentProviderError({ cause })
}

const authorizePaymentEffect = (token: string) =>
  Effect.tryPromise({
    try: (signal) => authorizePayment(token, { signal }),
    catch: toPaymentError,
  })

const placeOrder = (input: PlaceOrderRequest) => {
  return authorizePaymentEffect(input.paymentToken).pipe(
+    Effect.timeoutOrElse({
+      duration: "250 millis",
+      orElse: () => Effect.fail(new RequestTimedOut({ provider: "primary-pay" })),
+    }),
    Effect.retry({
      times: 2,
      while: (error) => error._tag === "ProviderUnavailable",
    }),
    Effect.map((authorization) =>
      httpResult(201, {
        orderId: `order-${input.customerId}`,
        status: "accepted",
        paymentProvider: authorization.provider,
      }),
    ),
    Effect.catchTags({
      PaymentDeclined: (error) =>
        Effect.succeed(
          httpResult(422, {
            error: error._tag,
            reason: error.reason,
          }),
        ),
      ProviderUnavailable: (error) =>
        Effect.succeed(
          httpResult(503, {
            error: error._tag,
            provider: error.provider,
          }),
        ),
+      RequestTimedOut: (error) =>
+        Effect.succeed(
+          httpResult(504, {
+            error: error._tag,
+            provider: error.provider,
+          }),
+        ),
      UnexpectedPaymentProviderError: (error) =>
        Effect.sync(() => {
          console.error("Unexpected payment provider failure", error.cause)
          return httpResult(502, {
            error: error._tag,
          })
        }),
    }),
  )
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

    const result = await Effect.runPromise(placeOrder(input))
    sendJson(result.status, result.body)
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

Wait for the server to reload, then send `tok_slow` again:

```text title="Expected response"
HTTP/1.1 504 Gateway Timeout
...

{"error":"RequestTimedOut","provider":"primary-pay"}
```

The server log proves that the Promise did not continue in the background:

```text title="Expected server log"
primary-pay authorization attempt 1
primary-pay authorization aborted
```

`Effect.tryPromise` supplied the `AbortSignal`, and
`Effect.timeoutOrElse` interrupted the operation. The signal reached the
ordinary Promise API without any timeout-specific code in `placeOrder`.

<TutorialCheckpoint
  number={9}
  total={11}
  title="One slow attempt has a fixed bound"
>
  The request first waits for the provider-controlled two seconds. With the
  timeout in place, one interrupted attempt returns a typed `504` after about
  250 ms.

  <AgentPrompt label="AI prompt: add an attempt timeout">

````text
Add a timeout that interrupts one slow provider attempt.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- Do not fix a failed observation or verification.

Before editing, make sure the server is available. If the connection is refused, start `node --watch server.ts` in a persistent terminal and wait for the listening message. Send `tok_slow` and measure its elapsed time. Verify that it returns status `201` after about two seconds. If it does not, stop without applying the patch and report the observed difference.

Apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
 class ProviderUnavailable extends Data.TaggedError("ProviderUnavailable")<{
   readonly provider: string
 }> {}

+class RequestTimedOut extends Data.TaggedError("RequestTimedOut")<{
+  readonly provider: string
+}> {}
+
 const toPaymentError = (cause: unknown) => {
@@
 const placeOrder = (input: PlaceOrderRequest) => {
   return authorizePaymentEffect(input.paymentToken).pipe(
+    Effect.timeoutOrElse({
+      duration: "250 millis",
+      orElse: () => Effect.fail(new RequestTimedOut({ provider: "primary-pay" })),
+    }),
     Effect.retry({
@@
       ProviderUnavailable: (error) =>
         Effect.succeed(
           httpResult(503, {
             error: error._tag,
             provider: error.provider,
           }),
         ),
+      RequestTimedOut: (error) =>
+        Effect.succeed(
+          httpResult(504, {
+            error: error._tag,
+            provider: error.provider,
+          }),
+        ),
       UnexpectedPaymentProviderError: (error) =>
*** End Patch
```

Wait for the watch process to reload the server. Send `tok_slow` again and verify:

1. Status `504` with body `{"error":"RequestTimedOut","provider":"primary-pay"}`.
2. The response arrives after about 250 ms instead of two seconds.
3. The server records one authorization attempt followed by `primary-pay authorization aborted`.

Reply with the before and after results in a concise pass or fail summary. On failure, report only the patch mismatch or observed difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 10. Retry the timeout within the same limit

The timeout is typed, so the retry decision can include it explicitly. Make
the following change:

```ts title="server.ts" collapse={5-52,106-175} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
import { Data, Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

interface HttpResult {
  readonly status: number
  readonly body: unknown
}

const httpResult = (status: number, body: unknown): HttpResult => ({
  status,
  body,
})

class UnexpectedPaymentProviderError extends Data.TaggedError(
  "UnexpectedPaymentProviderError",
)<{
  readonly cause: unknown
}> {}

class PaymentDeclined extends Data.TaggedError("PaymentDeclined")<{
  readonly reason: string
}> {}

class ProviderUnavailable extends Data.TaggedError("ProviderUnavailable")<{
  readonly provider: string
}> {}

class RequestTimedOut extends Data.TaggedError("RequestTimedOut")<{
  readonly provider: string
}> {}

const toPaymentError = (cause: unknown) => {
  if (cause instanceof PaymentDeclinedSdkError) {
    return new PaymentDeclined({ reason: cause.reason })
  }
  if (cause instanceof ProviderUnavailableSdkError) {
    return new ProviderUnavailable({ provider: cause.provider })
  }
  return new UnexpectedPaymentProviderError({ cause })
}

const authorizePaymentEffect = (token: string) =>
  Effect.tryPromise({
    try: (signal) => authorizePayment(token, { signal }),
    catch: toPaymentError,
  })

const placeOrder = (input: PlaceOrderRequest) => {
  return authorizePaymentEffect(input.paymentToken).pipe(
    Effect.timeoutOrElse({
      duration: "250 millis",
      orElse: () => Effect.fail(new RequestTimedOut({ provider: "primary-pay" })),
    }),
    Effect.retry({
      times: 2,
-      while: (error) => error._tag === "ProviderUnavailable",
+      while: (error) =>
+        error._tag === "ProviderUnavailable" || error._tag === "RequestTimedOut",
    }),
    Effect.map((authorization) =>
      httpResult(201, {
        orderId: `order-${input.customerId}`,
        status: "accepted",
        paymentProvider: authorization.provider,
      }),
    ),
    Effect.catchTags({
      PaymentDeclined: (error) =>
        Effect.succeed(
          httpResult(422, {
            error: error._tag,
            reason: error.reason,
          }),
        ),
      ProviderUnavailable: (error) =>
        Effect.succeed(
          httpResult(503, {
            error: error._tag,
            provider: error.provider,
          }),
        ),
      RequestTimedOut: (error) =>
        Effect.succeed(
          httpResult(504, {
            error: error._tag,
            provider: error.provider,
          }),
        ),
      UnexpectedPaymentProviderError: (error) =>
        Effect.sync(() => {
          console.error("Unexpected payment provider failure", error.cause)
          return httpResult(502, {
            error: error._tag,
          })
        }),
    }),
  )
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

    const result = await Effect.runPromise(placeOrder(input))
    sendJson(result.status, result.body)
  } catch (error) {
    console.error(error)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

Wait for the server to reload, then send `tok_slow` again. The HTTP response
remains `504`, but the server now records three bounded attempts:

```text title="Expected server log"
primary-pay authorization attempt 1
primary-pay authorization aborted
primary-pay authorization attempt 2
primary-pay authorization aborted
primary-pay authorization attempt 3
primary-pay authorization aborted
```

Every attempt has its own 250 ms limit. After the retry limit is exhausted, the
last `RequestTimedOut` continues to the existing HTTP handler.

<TutorialCheckpoint
  number={10}
  total={11}
  title="Timed-out attempts share the retry limit"
>
  The endpoint makes three interrupted attempts and then returns the existing
  typed `504`. Every attempt has the same 250 ms bound.

  <AgentPrompt label="AI prompt: retry timeouts">

````text
Include the typed timeout in the existing retry limit.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- After applying the change, verify the behavior but do not fix a failed verification.

Apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
     Effect.retry({
       times: 2,
-      while: (error) => error._tag === "ProviderUnavailable",
+      while: (error) =>
+        error._tag === "ProviderUnavailable" || error._tag === "RequestTimedOut",
     }),
*** End Patch
```

If the server refuses a connection, start `node --watch server.ts` in a persistent terminal, wait for the listening message, and retry. Leave the server active. If the server returns any HTTP response, do not restart it merely because the response is wrong.

Send `tok_slow` and verify:

1. Status `504` with body `{"error":"RequestTimedOut","provider":"primary-pay"}`.
2. The server records exactly three authorization attempts and each one is followed by `primary-pay authorization aborted`.

Reply only that the checkpoint succeeded, or report the patch mismatch or observed verification difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 11. Stop when the client disconnects

So far cancellation has travelled from Effect to the Promise. We can also
connect cancellation in the other direction.

Connect the HTTP response to the Effect operation so a client disconnect stops
the provider call. Make the following changes:

```ts title="server.ts" collapse={5-103,147-190} useDiffSyntax copyFinal
import { createServer } from "node:http"
import { setTimeout as sleep } from "node:timers/promises"
import { Data, Effect } from "effect"

interface PlaceOrderRequest {
  readonly customerId: string
  readonly paymentToken: string
}

interface HttpResult {
  readonly status: number
  readonly body: unknown
}

const httpResult = (status: number, body: unknown): HttpResult => ({
  status,
  body,
})

class UnexpectedPaymentProviderError extends Data.TaggedError(
  "UnexpectedPaymentProviderError",
)<{
  readonly cause: unknown
}> {}

class PaymentDeclined extends Data.TaggedError("PaymentDeclined")<{
  readonly reason: string
}> {}

class ProviderUnavailable extends Data.TaggedError("ProviderUnavailable")<{
  readonly provider: string
}> {}

class RequestTimedOut extends Data.TaggedError("RequestTimedOut")<{
  readonly provider: string
}> {}

const toPaymentError = (cause: unknown) => {
  if (cause instanceof PaymentDeclinedSdkError) {
    return new PaymentDeclined({ reason: cause.reason })
  }
  if (cause instanceof ProviderUnavailableSdkError) {
    return new ProviderUnavailable({ provider: cause.provider })
  }
  return new UnexpectedPaymentProviderError({ cause })
}

const authorizePaymentEffect = (token: string) =>
  Effect.tryPromise({
    try: (signal) => authorizePayment(token, { signal }),
    catch: toPaymentError,
  })

const placeOrder = (input: PlaceOrderRequest) => {
  return authorizePaymentEffect(input.paymentToken).pipe(
    Effect.timeoutOrElse({
      duration: "250 millis",
      orElse: () => Effect.fail(new RequestTimedOut({ provider: "primary-pay" })),
    }),
    Effect.retry({
      times: 2,
      while: (error) =>
        error._tag === "ProviderUnavailable" || error._tag === "RequestTimedOut",
    }),
    Effect.map((authorization) =>
      httpResult(201, {
        orderId: `order-${input.customerId}`,
        status: "accepted",
        paymentProvider: authorization.provider,
      }),
    ),
    Effect.catchTags({
      PaymentDeclined: (error) =>
        Effect.succeed(
          httpResult(422, {
            error: error._tag,
            reason: error.reason,
          }),
        ),
      ProviderUnavailable: (error) =>
        Effect.succeed(
          httpResult(503, {
            error: error._tag,
            provider: error.provider,
          }),
        ),
      RequestTimedOut: (error) =>
        Effect.succeed(
          httpResult(504, {
            error: error._tag,
            provider: error.provider,
          }),
        ),
      UnexpectedPaymentProviderError: (error) =>
        Effect.sync(() => {
          console.error("Unexpected payment provider failure", error.cause)
          return httpResult(502, {
            error: error._tag,
          })
        }),
    }),
  )
}

const server = createServer(async (request, response) => {
  const sendJson = (status: number, body: unknown): void => {
    response.writeHead(status, { "content-type": "application/json" })
    response.end(JSON.stringify(body))
  }

  if (request.method !== "POST" || request.url !== "/orders") {
    sendJson(404, { error: "NotFound" })
    return
  }

+  const controller = new AbortController()
+
+  response.once("close", () => {
+    if (!response.writableEnded) {
+      controller.abort()
+    }
+  })
+
  try {
    let body = ""
    for await (const chunk of request) {
      body += chunk
    }
    const input = JSON.parse(body) as PlaceOrderRequest

-    const result = await Effect.runPromise(placeOrder(input))
+    const result = await Effect.runPromise(placeOrder(input), {
+      signal: controller.signal,
+    })
    sendJson(result.status, result.body)
-  } catch (error) {
-    console.error(error)
+  } catch (cause) {
+    if (controller.signal.aborted) {
+      return
+    }
+    console.error(cause)
    sendJson(500, { error: "InternalServerError" })
  }
})

server.listen(3000, () => console.log("Listening on http://localhost:3000"))

// Tutorial fixture
// You will not modify the code below.

class PaymentDeclinedSdkError extends Error {
  readonly reason = "card declined"
}

class ProviderUnavailableSdkError extends Error {
  readonly provider = "primary-pay"
}

const attempts = new Map<string, number>()

const authorizePayment = async (
  token: string,
  { signal }: { readonly signal?: AbortSignal } = {},
) => {
  const attempt = (attempts.get(token) ?? 0) + 1
  attempts.set(token, attempt)

  console.log(`primary-pay authorization attempt ${attempt}`)

  if (token === "tok_declined") {
    attempts.delete(token)
    throw new PaymentDeclinedSdkError()
  }
  if (token === "tok_unexpected") {
    attempts.delete(token)
    throw new Error("undocumented payment provider failure")
  }
  if (token === "tok_flaky" && attempt < 3) {
    throw new ProviderUnavailableSdkError()
  }

  signal?.addEventListener("abort", () => console.log("primary-pay authorization aborted"), {
    once: true,
  })
  await sleep(token === "tok_slow" ? 2_000 : 80, undefined, { signal })

  attempts.delete(token)
  return { provider: "primary-pay" }
}
```

Wait for the server to reload, then ask `curl` to disconnect before the provider
responds:

```sh title="Disconnect during payment"
curl --max-time 0.1 -sS http://localhost:3000/orders \
  -H 'content-type: application/json' \
  -d '{"customerId":"customer-cancelled","paymentToken":"tok_slow"}'
```

`curl` reports that its deadline was reached. The server logs:

```text title="Expected server log"
primary-pay authorization attempt 1
primary-pay authorization aborted
```

Notice that no retry starts. The external signal interrupts the complete Effect
program, and interruption is not a typed `RequestTimedOut`. Effect propagates
the interruption to the signal already used by the Promise adapter.

<TutorialCheckpoint
  number={11}
  total={11}
  title="Client cancellation reaches the provider"
>
  When the client disconnects, the complete Effect operation stops, the provider
  receives an `AbortSignal`, and no retry begins after interruption.

  <AgentPrompt label="AI prompt: propagate cancellation">

````text
Connect an HTTP client disconnect to cancellation of the complete Effect operation.

Rules:
- Work only in `./handle-failures-without-guessing` relative to the current workspace root.
- Verify that the patch below applies cleanly to `server.ts` before changing anything. If it does not, stop and report that the file is not at the expected tutorial checkpoint.
- Modify only `server.ts`.
- Apply exactly the requested patch. Do not refactor or make additional improvements.
- After applying the change, verify the behavior but do not fix a failed verification.

Apply this patch to `server.ts`:

```diff
*** Begin Patch
*** Update File: server.ts
@@
   if (request.method !== "POST" || request.url !== "/orders") {
     sendJson(404, { error: "NotFound" })
     return
   }

+  const controller = new AbortController()
+
+  response.once("close", () => {
+    if (!response.writableEnded) {
+      controller.abort()
+    }
+  })
+
   try {
@@
-    const result = await Effect.runPromise(placeOrder(input))
+    const result = await Effect.runPromise(placeOrder(input), {
+      signal: controller.signal,
+    })
     sendJson(result.status, result.body)
-  } catch (error) {
-    console.error(error)
+  } catch (cause) {
+    if (controller.signal.aborted) {
+      return
+    }
+    console.error(cause)
     sendJson(500, { error: "InternalServerError" })
   }
 })
*** End Patch
```

If the server refuses a connection, start `node --watch server.ts` in a persistent terminal, wait for the listening message, and retry. Leave the server active. If the server returns any HTTP response, do not restart it merely because the response is wrong.

Ask the client to disconnect during the slow provider call by sending `tok_slow` with `curl --max-time 0.1`. Verify:

1. `curl` reports that its deadline was reached and receives no HTTP response.
2. The server logs exactly one authorization attempt followed by `primary-pay authorization aborted`.
3. No second authorization attempt starts.

Reply only that the checkpoint succeeded, or report the patch mismatch or observed verification difference. Do not show the diff and do not apply a correction.
````

  </AgentPrompt>
</TutorialCheckpoint>

## 12. Understand the final `try`/`catch`

The original `try`/`catch` handled every rejected Promise as `500`. In the final
server, all expected errors have already been converted to `HttpResult` values
by `Effect.catchTags` before `Effect.runPromise` is called.

The outer `catch` now has only two responsibilities:

- if the client interrupted the request, do not write a response;
- if the program contains an unexpected defect, log it and return a real `500`.

It does not reinterpret a defect as `PaymentDeclined`, `ProviderUnavailable`,
or `RequestTimedOut`. The familiar TypeScript boundary remains, but business and
operational decisions no longer depend on an untyped exception.

## What you built

The endpoint now preserves enough information to make each failure decision at
the right place:

- Promise rejections are translated once at the provider boundary;
- expected errors remain visible in the Effect error channel;
- HTTP responses are selected by error tag;
- only explicitly recoverable failures are retried;
- each slow attempt is interrupted;
- a disconnected client interrupts the complete operation;
- defects are not disguised as expected failures.

The tutorial used these Effect APIs:

- `Data.TaggedError` to define application errors;
- `Effect.tryPromise` to adapt the external Promise;
- `Effect.map` to transform a successful authorization;
- `Effect.catchTags`, `Effect.succeed`, and `Effect.sync` to handle expected
  errors;
- `Effect.retry` to apply a selective, bounded retry;
- `Effect.timeoutOrElse` and `Effect.fail` to represent a timeout;
- `Effect.runPromise` to execute the program at the existing TypeScript
  boundary.

`Effect.gen` would replace only the single `Effect.map` in this program, while
also introducing generator syntax and `yield*`. It would not reduce the error
handling, retry, timeout, or interoperability APIs, so this tutorial does not
use it.

## Related tutorials

- [Run independent work concurrently without losing control](/docs/v4/tutorials/run-independent-work-concurrently-without-losing-control)
- [Clean up resources after failure or cancellation](/docs/v4/tutorials/clean-up-resources-after-failure-or-cancellation)
