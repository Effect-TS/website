# Migration Report: v3 → v4 Docs Snippet Migration

Tracks progress against `specs/v3-to-v4-docs-snippet-migration.md`. Updated per directory (§9.1).

## Per-file table

| File | Total fences | Marked | Asserted | Class C exclusions |
| ---- | ------------ | ------ | -------- | ------------------- |
| `batching.mdx` | 17 | 8 | 2 | 9: 4 type-only shorthand (`Request<Value, Error>`, `RequestResolver<A>` x2, a resolver-effect type signature), 5 removed-API continuations (`Effect.withRequestBatching`, `Effect.withRequestCaching`, `Layer.setRequestCache`+`Request.makeCache` x2, a plain caching-disabled continuation) |
| `configuration.mdx` | 23 | 9 | 2 | 14: 11 real-env-var fences left as copy-paste-and-run-with-tsx scripts (primitives/defaults/redacted/secret-number/array/hashSet/hashMap/validate/App.ts/nested-namespace/constantCase — code migrated where APIs changed, not marked, since marking would make the doctest run depend on unset env vars), 1 deprecated `Secret` example (module removed entirely in v4, left unchanged), 2 removed path/sequence-delimiter-customization demos with no v4 equivalent (left unchanged) |
| `runtime.mdx` | 7 | 6 | 0 | 1: `Runtime.defaultRuntime`/`Runtime.runPromise` example — the `Runtime<R>` data type and its `run*` functions are removed entirely in v4, no replacement, left unchanged |
| `getting-started/building-pipelines.mdx` | 19 | 11 | 0 | 8: pseudo-code syntax-shorthand fences for `pipe`/`map`/`flatMap`/`andThen`/`all`/the `pipe` method (x7, all unchanged APIs) plus one incomplete `flatMap` ignored-effect warning snippet |
| `getting-started/control-flow.mdx` | 25 | 19 | 0 | 6: 4 pseudo-code syntax/desugaring illustrations of the removed `Effect.loop`/`Effect.iterate` signatures (left as historical illustration, nothing to migrate to under the same name), 1 `Effect.all` tuple syntax shorthand, 1 `mode: "validate"` example (option removed in v4, no replacement) |
| `getting-started/creating-effects.mdx` | 22 | 17 | 4 | 5: 2 pseudo-code syntax shorthand (`try`/`catch`, `Effect.suspend(() => effect)`), 1 `Effect.callback<Buffer, Error>` type-annotation illustration snippet, 1 filesystem-writing interruption-cleanup demo (real side effects, code fixed but left unmarked per F6), 1 stack-overflow crash demo (`blowsUp`) |
| `getting-started/devtools.mdx` | 1 | 0 | 0 | 1: `NodeRuntime.runMain` process-entry-point demo with an `Effect.forever` loop — would hang the doctest run, left unmarked |
| `getting-started/importing-effect.mdx` | 0 (no changes needed) | — | — | n/a — install instructions and trivial single-line import illustrations only, no API usage to migrate |
| `getting-started/installation.mdx` | 3 (ts/js) | 3 | 0 | 0 — a `diff lang="tsx"` snippet is out of spec scope (not a ts/js fence) |
| `getting-started/introduction.mdx` | 0 (no changes needed) | — | — | n/a — pure prose/table, no code fences at all |
| `getting-started/running-effects.mdx` | 8 | 5 | 0 | 3: two `Cause`/`Exit` async-defect demos (console output includes a live `FiberImpl` object, not stable/comparable) and the `runFork` background-execution example (fire-and-forget via `setTimeout`+`Fiber.interrupt`, not observable within one synchronous doctest tick) |
| `getting-started/the-effect-type.mdx` | 1 | 1 | 0 | 0 |
| `getting-started/using-generators.mdx` | 9 | 7 | 0 | 2: the deprecated `$`-adapter example (removed overload, no replacement) and one dead-code-after-failure example whose two possible fixes each trigger a different strict-mode compiler error without altering its teaching point |
| `getting-started/why-effect.mdx` | 2 | 2 | 2 | 0 |
| `testing/testclock.mdx` | 4 | 4 | 4 (pre-existing `node:assert`, not `// =>`) | 0 |
| `state-management/ref.mdx` | 7 | 5 | 0 | 2: real `process.stdin` reads (would hang the test runner) |
| `state-management/subscriptionref.mdx` | 5 | 4 | 0 | 1 pseudo-code type illustration |
| `state-management/synchronizedref.mdx` | 1 | 1 | 0 | 0 |
| `caching/cache.mdx` | 3 | 0 | 0 | 3: 2 type-only pseudo-code, 1 built around `Cache.CacheStats` (removed, no replacement) |
| `caching/caching-effects.mdx` | 5 | 4 | 0 | 1: `Effect.cachedFunction` removed, no replacement |
| `behaviour/equivalence.mdx` | 3 | 2 | 0 | 1 type-only interface declaration |
| `behaviour/order.mdx` | 11 | 10 | 0 | 1 type-only interface declaration |
| `trait/equal.mdx` | 8 | 8 | 0 | 0 |
| `trait/hash.mdx` | 1 | 1 | 0 | 0 |
| `sink/concurrency.mdx` | 2 | 0 | 0 | 2: `Sink.zip({concurrent:true})`/`Sink.race` — concurrent/racing sink behavior removed, no v4 replacement |
| `sink/creating.mdx` | 19 | 19 | 16 | 0 |
| `sink/introduction.mdx` | 3 (1 ts + 2 text) | 1 | 1 | 0 |
| `sink/leftovers.mdx` | 2 | 2 | 2 | 0 |
| `sink/operations.mdx` | 3 | 2 | 2 | 1: `Sink.filterInput` removed, Stream-level-only workaround, not a mechanical Sink rename |
| `code-style/branded-types.mdx` | 14 | 2 | 0 | 12: pedagogical preamble, `@errors:` compile-error demos, intentional uncaught-throw demos |
| `code-style/do.mdx` | 3 | 3 | 0 | 0 |
| `code-style/dual.mdx` | 6 (no changes needed) | — | — | n/a — all pseudo-code/declare-only, one real signature shown is byte-identical in v4 |
| `code-style/guidelines.mdx` | 3 (no changes needed) | — | — | n/a — all APIs used confirmed unchanged; main fence is an intentional infinite-repeat+CTRL-C demo, left unmarked |
| `code-style/pattern-matching.mdx` | 12 | 10 | 0 | 2: `@errors:` compile-error demos |
| `resource-management/introduction.mdx` | 4 | 4 | 0 | 0 |
| `resource-management/scope.mdx` | 18 | 16 | 0 | 2: type-signature display + incomplete continuation fragments |
| `requirements-management/default-services.mdx` | 2 | 2 | 0 | 0 |
| `requirements-management/layer-memoization.mdx` | 5 | 3 | 0 | 1: `Layer.memoize` removed, no replacement (v4 auto-memoizes across `Effect.provide` instead) |
| `requirements-management/services.mdx` | 18 | 10 | 0 | 8 |
| `requirements-management/layers.mdx` | ~35 | 18 | 0 | ~17, including 1 `accessors: true` fence (removed, no replacement) |
| `observability/logging.mdx` | 26 | 26 | 0 | 0 |
| `observability/metrics.mdx` | 14 | 14 | 0 | 0 |
| `observability/supervisor.mdx` | 1 (no changes committed) | 0 | 0 | 1: entire `effect/Supervisor` module removed, no mechanical replacement (v4 uses `FiberSet`/`FiberMap` instead — a different pattern, not a rename) |
| `observability/tracing.mdx` | 8 | 1 | 0 | 7: import `@effect/opentelemetry`/`@opentelemetry/*`/`@sentry/opentelemetry`, none installed in this repo — code migrated, left unmarked (would break G1/G2 for reasons outside doc content) |
| `ai/getting-started.mdx` | 12 | 1 | 0 | 11: import `@effect/ai-openai`/`@effect/ai-anthropic`, neither installed in this repo |
| `ai/introduction.mdx` | 0 (no changes needed) | — | — | n/a — zero code fences |
| `ai/planning-llm-interactions.mdx` | 4 | 0 | 0 | 4: same unresolvable-provider-import issue |
| `ai/tool-use.mdx` | 5 | 4 | 4 | 1: real LLM call + unresolvable provider import |
| `platform/command.mdx` | 7 | 1 | 1 | 6: real process-spawn demos (`NodeRuntime.runMain`) |
| `platform/file-system.mdx` | 3 | 2 | 0 | 1: reads a real file |
| `platform/introduction.mdx` | 3 | 1 | 0 | 2: `NodeRuntime.runMain`/`BunRuntime.runMain` |
| `platform/key-value-store.mdx` | 3 | 3 | 0 | 0 |
| `platform/path.mdx` | 2 | 1 | 0 | 1: `NodeRuntime.runMain` |
| `platform/platformlogger.mdx` | 2 | 0 | 0 | 2: both write a real file |
| `platform/runtime.mdx` | 5 (no changes needed) | 0 | 0 | 1: `NodeRuntime.runMain`'s `disablePrettyLogger` option removed, no replacement, left unchanged |
| `platform/terminal.mdx` | 4 | 1 | 0 | 3: real stdin/stdout |
| `concurrency/basic-concurrency.mdx` | 24 | 16 | 0 | 8: `concurrency:"inherit"`+`Effect.withConcurrency` (removed, no replacement) x2, `Effect.disconnect` x3, `Effect.raceWith` x1 (removed, no single-symbol replacement), plus type-only |
| `concurrency/deferred.mdx` | 6 | 6 | 0 | 0 |
| `concurrency/fibers.mdx` | 13 | 13 | 0 | 2: infinite daemon-fiber demos |
| `concurrency/latch.mdx` | 1 | 1 | 0 | 0 |
| `concurrency/pubsub.mdx` | 7 | 7 | 0 | 1: "PubSub as an Enqueue" — `PubSub<A>` no longer extends `Queue.Enqueue<A>`, no valid replacement declaration |
| `concurrency/queue.mdx` | 15 | 15 | 0 | 1: `Queue.awaitShutdown`+`Queue.shutdown` combo — empirically confirmed broken when mechanically renamed |
| `concurrency/semaphore.mdx` | 3 | 3 | 0 | 0 |
| `scheduling/introduction.mdx` | 0 (ts/js) | — | — | n/a — only a `text` diagram |
| `scheduling/built-in-schedules.mdx` | 8 | 8 | 0 | 0 |
| `scheduling/cron.mdx` | 7 | 7 | 0 | 0 |
| `scheduling/examples.mdx` | 4 | 2 | 0 | 2: real network IO (fetch to dummyjson.com), per F6 — teaching point requires real execution |
| `scheduling/repetition.mdx` | 6 | 6 | 0 | 0 |
| `scheduling/schedule-combinators.mdx` | 8 | 8 | 0 | 0 |
| `stream/introduction.mdx` | 2 | 2 | 0 | 0 |
| `stream/creating.mdx` | 28 | 22 | 0 | 6: 3 removed-API (`unfoldChunkEffect`/`paginateChunkEffect` trio — pedagogical contrast no longer exists, entirely untouched for human rewrite), 1 dead-error demo, 2 type-decl/Class C |
| `stream/consuming-streams.mdx` | 4 | 4 | 0 | 0 |
| `stream/error-handling.mdx` | 13 | 11 | 0 | 2: pre-existing crash demos |
| `stream/operations.mdx` | 47 | 42 | 0 | 5: `zipAll`/`zipAllWith` x2 (no deterministic replacement), 1 real-time-too-slow buffer demo, 2 pre-existing crash/interactive demos |
| `stream/resourceful-streams.mdx` | 3 | 3 | 0 | 0 |
| `error-management/error-accumulation.mdx` | 6 | 5 | 0 | 1: `Effect.validateFirst` removed, no replacement |
| `error-management/error-channel-operations.mdx` | 13 | 13 | 0 | 0 |
| `error-management/expected-errors.mdx` | 16 | 16 | 0 | 0 |
| `error-management/fallback.mdx` | 4 | 4 | 0 | 0 |
| `error-management/matching.mdx` | 5 | 5 | 0 | 0 |
| `error-management/parallel-and-sequential-errors.mdx` | 4 | 3 | 0 | 1: `Effect.parallelErrors` removed, no faithful replacement |
| `error-management/retrying.mdx` | 4 | 4 | 0 | 0 |
| `error-management/sandboxing.mdx` | 1 | 1 | 0 | 0 |
| `error-management/timing-out.mdx` | 9 | 9 | 0 | 0 |
| `error-management/two-error-types.mdx` | 1 (no changes needed) | 0 | 0 | 1: intentionally-incomplete pseudo-code, no v3 API present |
| `error-management/unexpected-errors.mdx` | 6 | 6 | 0 | 1 inline mention: `Effect.catchSomeDefect` removed, no replacement |
| `error-management/yieldable-errors.mdx` | 2 | 2 | 0 | 0 |

## Unresolved items

- **API names with no v4 replacement**: `Effect.withRequestBatching`, `Effect.withRequestCaching`, `Layer.setRequestCache`, `Request.makeCache` (`batching.mdx`). Confirmed absent from `node_modules/effect/src/{Effect,Layer,Request}.ts`. A differently-shaped resolver-level caching API exists (`RequestResolver.asCache`/`withCache`/`persisted`, `@since 4.0.0`), but adopting it moves caching from the call site to the resolver's definition site — a structural rewrite of the surrounding example, not a mechanical migration. Left the affected fences (already Class C, unmarked, non-twoslash continuations) byte-for-byte unchanged; inline code spans naming these removed APIs in prose also left unchanged since there's no v4 name to rename to.
- **Link targets that couldn't be verified**: none found yet.
- **Table-vs-installed discrepancies**: none found yet.
- **Prose now imprecise after a source-of-truth-justified code fix, left unchanged per one-edit rule**: `batching.mdx`, "Declaring Resolvers" section — "A `RequestResolver` requires an environment `R`..." is no longer accurate since v4's `RequestResolver<A>` dropped its second (`R`) type parameter, but this is prose, not one of the spec's two carved-out exceptions to the verbatim-prose rule. Flagging for human review/decision rather than silently editing prose.
- **`configuration.mdx` — removed feature, no v4 replacement**: `ConfigProvider`'s path/sequence-delimiter customization. v3's `ConfigProvider.fromMap`/`fromEnv` accepted `pathDelim`/`seqDelim` options; v4's `ConfigProvider.fromEnv({env, preserveEmptyStrings})` hardcodes `_` as the only path delimiter with no customization option, and has no sequence-delimiter concept at all (array/record separators moved to `Config.Array`/`Config.Record`'s own `separator`/`keyValueSeparator` options, which are per-value, not per-provider). "### Handling Nested Configuration Values" (demonstrates `ConfigProvider.fromMap`'s default `.` separator) and "### Loading Configuration from Environment Variables" > "Changing the Path Delimiter" (explicit `pathDelim`/`seqDelim` options) are left byte-for-byte unchanged — there's no way to reproduce either demo's actual teaching point in v4. (One adjacent fence, "Customizing the Path Delimiter", asked for `_` as the separator, which is already v4's hardcoded default, so that one *was* migrated — it just no longer needs an explicit option.)
- **`configuration.mdx` — removed module, no replacement (already flagged as deprecated toward one)**: `effect/Secret` doesn't exist in v4 at all — not simply still-deprecated, fully removed. The "## Deprecations > Secret" section's example fence (`secret.ts`) can't even type-check any more, left byte-for-byte unchanged. `Config.redacted`/`Redacted` (used earlier in the same file) is already documented in the deprecation notice as the intended replacement, so a human may want to delete this whole section now that the deprecated API is gone rather than keep documenting a nonexistent module — that's a deletion decision, not mine to make.
- **`configuration.mdx` — runtime bug found via doctest gate, not a rename**: v3-style `provider1.pipe(ConfigProvider.orElse(() => provider2))` (passing a thunk) throws `TypeError: second.load is not a function` at runtime in v4 — `ConfigProvider.orElse(that)`'s `that` parameter is the fallback provider value directly, not a lazy thunk. Fixed to `ConfigProvider.orElse(provider2)`.
- **`runtime.mdx` — removed type/module, no replacement**: `Runtime<R>` (the data type this file's intro paragraphs are structured around: "consists of three main components: Context<R>, FiberRefs, RuntimeFlags") no longer exists in v4 — confirmed via `specs/_references/runtime.md` and `node_modules/effect/src/Runtime.ts`, which now exports only process-lifecycle utilities (`Teardown`, `defaultTeardown`, `makeRunMain`). `Runtime.defaultRuntime`/`Runtime.runPromise` are gone with it. Only one fence in the file actually constructs/uses a `Runtime` value directly ("Running an Effect Using the Default Runtime") — left unchanged. The rest of the file's prose describes the `Runtime<R>` concept without a matching removed-API code fence, so it's left untouched per the one-edit rule even though the underlying type no longer exists — flagged here for human review since this affects the file's framing more broadly than a single fence.
- **Prose now imprecise after a source-of-truth-justified code fix, left unchanged per one-edit rule**: `runtime.mdx`, "Effect.Tag" section — "the fields of the service...are turned into static properties of the `Notifications` class" describes v3's removed accessor-proxy behavior; `Context.Service` doesn't do this (use `.use()`/`.useSync()` instead, per `specs/_references/services.md`). Flagged for human review rather than silently edited.
- **`getting-started/building-pipelines.mdx` — behavior change, no removed symbol**: `Effect.andThen`'s accepted second-argument shapes were trimmed from 6 (value / fn→value / Promise / fn→Promise / Effect / fn→Effect) to 2 (Effect / fn→Effect) — confirmed by reading `Effect.andThen`'s full overload list in `node_modules/effect/src/Effect.ts` (only 4 signatures: 2 pipeable + 2 data-first, both restricted to the Effect-returning cases). Fixed 4 call sites where the callback returned a plain value to use `Effect.map` instead. The "Option and Either with andThen" prose subsection and the Cheatsheet table's `andThen` row still describe the old 6-case polymorphism — left unchanged per one-edit rule (prose/table, not a code fence), flagged for human review.
- **`getting-started/building-pipelines.mdx` — reference doc discrepancy**: `specs/_references/yieldable.md` describes an `.asEffect()` method for converting a `Yieldable` (`Option`, `Result`, etc.) into an `Effect` explicitly. This method does not exist anywhere in the installed `effect@4.0.0-beta.104` source (`grep -rl asEffect node_modules/effect/src` returns nothing). The actual conversion functions, confirmed directly in `node_modules/effect/src/Effect.ts` with their own JSDoc examples, are `Effect.fromOption` and `Effect.fromResult`. Installed source wins per spec §4; used `fromOption`/`fromResult` in the migrated fences.
- **`getting-started/control-flow.mdx` — removed functions, no direct replacement**: `Effect.if`, `Effect.loop`, `Effect.iterate`, `Effect.unless`, `Effect.unlessEffect` are all gone in v4 (confirmed absent from `node_modules/effect/src/Effect.ts` and listed as removed in `specs/_references/v3-to-v4.md`, which documents the migration path as "rewrite around an explicit `Effect.gen` loop" for `loop`/`iterate`). Rewrote the runnable examples using `Effect.gen`; left the syntax-illustration pseudo-fences for `loop`/`iterate` unchanged since there's no v4 signature to illustrate under those names. `Effect.unless`/`Effect.unlessEffect` have no code fence in this file, only a prose mention — left unchanged.
- **`getting-started/control-flow.mdx` — behavior change, no removed symbol**: `Effect.when`/`Effect.whenEffect` were consolidated into a single `Effect.when(effect, condition: Effect<boolean, E, R>)` — v3's plain-predicate `Effect.when(() => boolean)` no longer exists as a separate overload; the condition must always be an `Effect<boolean>`. Fixed by wrapping predicates in `Effect.succeed(...)`.
- **`getting-started/control-flow.mdx` — removed option, no replacement**: `Effect.all`'s third `mode: "validate"` option (`Option`-based partial-failure collection) is gone — the option's type only accepts `"default" | "result"` now. Left that example's fence and prose mention unchanged.
- **`getting-started/creating-effects.mdx`**: no removed-API-with-no-replacement issues found; all changes were confirmed straightforward renames (`UnknownException`→`UnknownError`, `Effect.async`→`Effect.callback`, `Effect.fork`→`Effect.forkChild`).
- **`getting-started/devtools.mdx` — out-of-scope staleness, flagged for human review**: the 4 shell install snippets ("npm/pnpm/yarn/bun install @effect/experimental") are no longer necessary — `DevTools` moved into the core `effect` package (`effect/unstable/devtools`, confirmed in `node_modules/effect/src/unstable/devtools/index.ts`) and no longer requires a separate package install. Left unchanged because `sh` fences are outside the spec's scope (§2 restricts migration to `ts`/`js` fences only), but this is real, user-facing staleness a human should probably fix directly (delete the install step, or note DevTools now ships with `effect`).
- **`getting-started/running-effects.mdx` — inspect-format changes, confirmed empirically**: `Cause`'s console/inspect shape changed from a flat single-reason node (`{_id:"Cause", _tag:"Fail", failure: E}`) to an array of reasons (`{_id:"Cause", failures:[{_tag:"Fail", error: E}]}` — note the field also renamed `failure`→`error`). `Effect.runSync` throwing a failure (and `Effect.runPromise`'s rejection) now surfaces the raw failure value directly, not a `"(FiberFailure) Error: ..."` wrapper. The async-in-runSync defect changed from a `FiberRuntime`-wrapped `AsyncFiberException` to a plain object tagged `AsyncFiberError` with a different message. All confirmed by actually running the code against the installed package, not inferred from types.
- **`getting-started/the-effect-type.mdx` — behavior change**: `Effect.Context<T>` (utility type) renamed to `Effect.Services<T>`. Separately, `Effect.Effect.Success<T>`/`Effect.Effect.Error<T>` (the doubly-nested access pattern) no longer resolves — v4's `Success`/`Error`/`Services` utility types are plain top-level exports of `Effect.ts`, not nested inside a `namespace Effect {}` block merged with the `Effect` interface, so the correct v4 access is one level shallower: `Effect.Success<T>` etc. Caught via the G2 gate (TS2724).
- **`getting-started/using-generators.mdx` — behavior change**: `Effect.gen`'s `this`-binding overload changed call shape from `Effect.gen(this, function* () {...})` (bare first argument) to `Effect.gen({ self: this }, function* () {...})` (options object). Confirmed directly against the overload signatures in `node_modules/effect/src/Effect.ts`.
- **`getting-started/using-generators.mdx` — removed API, no replacement**: the deprecated `$`-adapter overload of `Effect.gen` (`function* ($) { yield* $(effect) }`) — v4's `Effect.gen` only has the plain and `{self}`-options overloads confirmed above, no adapter parameter. The v3 prose already predicted this ("anticipated to be removed in the upcoming major release of Effect") — v4 is that release.
- **Infra fix (corpus-wide, found via `getting-started/why-effect.mdx`)**: extracted doctest snippets with no `import`/`export` statement are TypeScript "scripts", not modules — their top-level declarations join one global scope shared by every other script-style snippet in the same `tsc` project. Two unrelated docs that both declare `const divide = ...` with no imports (`why-effect.mdx`, `creating-effects.mdx`) collided with "Cannot redeclare block-scoped variable" once both were marked runnable. Fixed in `scripts/extract-doctest-snippets.mjs`: any extracted snippet with no import/export gets a trailing `export {}` appended, forcing isolated module scope. Verified this eliminated all TS2451 "Cannot redeclare" errors corpus-wide.
- **`state-management/subscriptionref.mdx` — behavior change, major**: `SubscriptionRef` no longer structurally extends `SynchronizedRef`/`Ref` in v4 (confirmed in `node_modules/effect/src/SubscriptionRef.ts`). `ref.changes` (property) → `SubscriptionRef.changes(ref)` (plain function); code that typed a param as `Ref.Ref<number>` and passed a `SubscriptionRef` no longer type-checks, needed retyping to `SubscriptionRef.SubscriptionRef<number>` and `Ref.update`→`SubscriptionRef.update` throughout. `Stream.runCollect` also now returns `Array<A>` instead of `Chunk<A>`, so this file's `Example Output` block (`{_id:'Chunk',...}`) is stale — left untouched (documentation comment, not an assertion), flagged.
- **`caching/cache.mdx` — removed API, no replacement**: `Cache.CacheStats` fully removed in v4 (confirmed absent from `Cache.ts`); the file's only narrative example reads `cache.cacheStats.hits`/`.misses`, left byte-for-byte unchanged including an otherwise-mechanical `cache.get()`→`Cache.get()` rename it also contains, since the block can't compile without the removed type.
- **`caching/caching-effects.mdx` — removed API, no replacement**: `Effect.cachedFunction` removed (the migration guide suggests "use `Cache`" but that's a structural rewrite, not a mechanical rename). Also `Effect.once`→`Effect.cached`, `Effect.repeatN(x,n)`→`Effect.repeat(x,{times:n})`.
- **`caching/cache.mdx` — table-vs-installed discrepancy**: `specs/_references/v3-to-v4.md`'s `effect/Cache` section only lists type/namespace renames and omits v3's instance-method→v4 module-function renames entirely (`get`, `has` (was `contains`), `refresh`, `invalidate`, `invalidateAll`, `size`, plus new `getOption`/`getSuccess`/`set`/`invalidateWhen`/`keys`/`values`/`entries`) — verified directly against installed `Cache.ts` per spec §4 priority order.
- **`trait/equal.mdx` — removed API, contradicts an earlier session's own note**: `Data.struct` is fully removed in v4 (confirmed absent from `Data.ts`; `specs/_references/v3-to-v4.md`: "Data.struct -> none: use an ordinary object or {...value}; plain objects are structurally equal in v4"). An earlier session's "known-good-patterns" note had incorrectly claimed `Data.struct` was unchanged — corrected here. Replaced with plain object literals in 3 fences.
- **`trait/equal.mdx` — behavior change, major, core teaching narrative inverted**: v4 makes `Equal.equals` structural-by-default for plain objects/arrays/`Map`/`Set` (v3 was reference-equality by default) — confirmed by executing code against the installed package. This flips the correct answer in several `// Output:` comments this file teaches from (e.g. the opening example's `false`→`true`; the `HashSet`-dedup "pitfall" section's whole point no longer holds, `2`→`1`). Per spec, `// Output:` comments are left byte-for-byte even when now wrong — flagged prominently for a human editor, including one spot where two adjacent, now-identical lines claim opposite results.
- **`code-style/branded-types.mdx` — removed API, no replacement**: `Brand.refined`/`Brand.error` fully removed (`Brand.ts` now exports only `nominal`/`make`/`check`/`all`). Migrated 4 call sites from `Brand.refined(pred, errFn)` to `Brand.make((n) => pred || errMsg)`; verified real v4 throw behavior by executing the migrated code (`BrandError`, not v3's array-of-`{message}`) and rewrote the `// throws [...]` comments to match the actual `.toString()` output.
- **`code-style/pattern-matching.mdx` — behavior change**: `Match.either` removed, replaced by `Match.result`. Verified real v4 console output by execution: Success prints `{_id:'Result',_tag:'Success',value:...}` (display key `value`, not `.success`), Failure prints `{_id:'Result',_tag:'Failure',failure:...}` — rewrote the `// Output:` comments to match.
- **`resource-management/introduction.mdx` — removed API, no replacement**: `Exit.getOrElse` absent from `Exit.ts`; rewritten as `Exit.match(exit, {onSuccess: identity, onFailure: String})` per the reference table.
- **`resource-management/scope.mdx` — behavior change + real v4 bug found**: `Scope.extend`→`Scope.provide`. Found and worked around a genuine bug in installed `effect@4.0.0-beta.104`: `Effect.provideService(tag, undefined)` silently no-ops (`provideServiceImpl` does `if (prev === implementation) return s`, and a missing key's `Map.get` also returns `undefined` — indistinguishable from "already provided as undefined"). Reproduced in isolation; worked around with a separate `Effect.provide(Layer.succeed(Tag, undefined))` pipe step. Also: this file's `` ```ansi `` output blocks showing `Effect.either`/`Either` console output (`_id:'Either',_tag:'Right',right:...`) are now stale (`_id:'Result',_tag:'Success',success:...`) — non-ts/js fence, out of edit scope, flagged.
- **`requirements-management/layer-memoization.mdx` — removed API, no replacement**: `Layer.memoize` removed (v4 auto-memoizes across `Effect.provide` calls instead of requiring an explicit call). This also means the file's "No Memoization When Providing Locally" example's real v4 output changed (would now log once, not twice, due to the new auto-memoization) — left its `/* Output: */` comment untouched per spec, flagged.
- **`requirements-management/layers.mdx` — removed API + major restructure**: `Effect.Service` (auto-generating a `.Default` layer) fully removed; every use rebuilt as `Context.Service<Self>()(id, {make})` with an explicit `static readonly layer = Layer.effect(this, this.make).pipe(Layer.provide(...))`. `new MyService({...})` no longer works (constructor takes `never`) — replaced with `MyService.of({...})`. `Layer.catchAll`→`Layer.catch`; `Layer.orElse` removed, folded into `catch`; `@effect/platform`'s `FileSystem` import moved to `effect`. The `accessors: true` option and the file's "Effect.Service vs Context.Tag" comparison table/prose describe a duality that no longer exists (both merged into `Context.Service`) — left unchanged, flagged for human review.
- **`observability/logging.mdx` — extensive Logger restructuring**: `Effect.either`/`Either.isLeft/.left/.right`→`Effect.result`/`Result.isFailure/.failure/.success`; `Logger.withMinimumLogLevel(effect,level)`→`Effect.provideService(effect, References.MinimumLogLevel, level)`; `Logger.minimumLogLevel(level)`→`Layer.succeed(References.MinimumLogLevel, level)`; `Logger.replace`→`Logger.layer([x, Logger.tracerLogger])`; `Logger.logFmt`/`.pretty`/`.structured`/`.json`→`Logger.layer([Logger.console*, Logger.tracerLogger])`; `LogLevel.Debug/.None/.Warning`→string literals `"Debug"/"None"/"Warn"`; `Layer.setConfigProvider`→`ConfigProvider.layer`; `ConfigProvider.fromMap`→`ConfigProvider.fromUnknown`. Built-in Loggers section's output blocks are stale (`logLevel` field→`level`) — flagged.
- **`observability/metrics.mdx` — behavior change, major**: metrics are no longer directly callable in v4 — every `metric(effect)` call site became `effect.pipe(Effect.trackSuccesses(metric))` (or `Effect.trackDuration` for timers). `MetricBoundaries.linear/exponential`→`Metric.linearBoundaries/exponentialBoundaries`; `Metric.summary({name,...,error})`→`Metric.summary(name, {...})` with the `error` margin option dropped (removed, no replacement, flagged); `Metric.tagged`→`Metric.withAttributes`. Deviated from the reference table's suggested `Metric.withAttributes` replacement for `Effect.tagMetrics` in favor of the more faithful `Effect.provideService(effect, Metric.CurrentMetricAttributes, {...})`, verified against source — documented as a deliberate discrepancy.
- **`observability/supervisor.mdx` — removed module, no replacement**: the entire `effect/Supervisor` module (`Supervisor.track`, `Effect.supervised`, `effect/FiberStatus`) removed with no mechanical replacement — v4 uses structured concurrency + explicit `FiberSet`/`FiberMap` instead, a different pattern, not a rename. Confirmed via reference doc and zero hits anywhere in installed source. Whole file left unchanged, no commit.
- **`ai/getting-started.mdx` — behavior change**: `Model` is no longer directly yieldable in v4 (Layer-only) — `yield* Gpt4o` replaced with `yield* Gpt4o.captureRequirements`, confirmed against `unstable/ai/Model.ts`. This specific change is unverified by gates (the surrounding fence imports an uninstalled provider package), flagged for human review.
- **`platform/*` — module relocation map, verified against installed source**: `FileSystem`/`Path`/`Terminal` promoted into core `effect` (method surfaces unchanged). `Command`/`CommandExecutor` reshaped and moved to `effect/unstable/process/{ChildProcess,ChildProcessSpawner}` — not a pure rename: free functions (`string`/`lines`/`exitCode`/`env`/`runInShell`/`feed`/`start`/`stdout`) replaced by service methods and `make`/`setEnv`/`setCwd` combinators. `KeyValueStore` moved to `effect/unstable/persistence/KeyValueStore`; `get`/`getUint8Array` now return `T | undefined` instead of `Option<T>` (real behavior change, confirmed by execution — one fence's `Output:` comment is now stale, flagged). `PlatformLogger` folded into `effect/Logger` (`toFile` moved there; `Logger.zip` removed, use `Logger.layer([...])`). `@effect/platform-node`'s `NodeContext` bundle layer replaced by `NodeServices.layer`. `Terminal.QuitException`→`Terminal.QuitError`; `@effect/platform/Error`→`effect/PlatformError`.
- **`platform/runtime.mdx` — removed API, no replacement**: `NodeRuntime.runMain`'s `disablePrettyLogger` option removed (confirmed by direct type-check failure); the one fence using it left byte-for-byte unchanged.
- **`concurrency/` — extensive fiber-primitive renames, verified against installed source**: `Effect.fork`→`forkChild`, `Effect.forkDaemon`→`forkDetach` throughout `fibers.mdx`/`deferred.mdx`/`queue.mdx`; `RuntimeFiber`→`Fiber` in type annotations; `Fiber.zip`/`Fiber.orElse`/`Fiber.interruptFork` all removed (rewrote `Fiber.zip(a,b)`→`Effect.zip(Fiber.join(a), Fiber.join(b))`; `fiber.interruptFork()`→`fiber.interruptUnsafe()`); `Effect.makeLatch`→`Latch.make` (own module now); `Effect.makeSemaphore`→`Semaphore.make` (own module now); `PubSub.subscribe` now returns `Subscription<A>`, not `Queue.Dequeue`; `SubscriptionRef`'s `ref.changes` property→`SubscriptionRef.changes(ref)` function (consistent with the same finding in `state-management/subscriptionref.mdx`).
- **`concurrency/basic-concurrency.mdx` — removed API, no replacement**: `concurrency: "inherit"` + `Effect.withConcurrency` — the `Concurrency` type is now just `number | "unbounded"`, no ambient/inherited concurrency mechanism exists in v4. Also `Effect.disconnect` and `Effect.raceWith` — no single-symbol replacement, the reference guide only sketches a redesign path (fork explicitly + inspect `Exit`s).
- **`concurrency/queue.mdx` — table-vs-installed discrepancy**: `specs/_references/v3-to-v4.md` claims `Queue.takeUpTo` has "no direct bounded batch helper" replacement; installed `Queue.takeBetween(queue, 0, n)` is a verified-working direct equivalent (confirmed `min=0` never waits, by reading source). Conversely, `Queue.awaitShutdown`→`Queue.await` (a real rename per the same table) was verified BROKEN when combined with `Queue.shutdown` in this file's specific example (rejects with an interrupt on execution) — left that one fence unchanged despite the rename being real elsewhere.
- **`concurrency/` — inspect-format discrepancies, found by execution, not documented anywhere**: `Cause`'s internal property is `.reasons`, but console/JSON serialization renders it under the key `"failures"` (matches the `{_id:'Cause', failures:[...]}` shape already confirmed independently in `getting-started/running-effects.mdx`). `Result`'s `Success`/`Failure` inspect output is asymmetric: `Success` renders `{_id:'Result',_tag:'Success',value:...}` (field `value`) while `Failure` renders `{...,failure:...}` (field `failure`). **Resolved**: this contradicted an earlier guess in `getting-started/control-flow.mdx` (used `success:` instead of `value:` for the `Success` case) — re-verified directly by executing `Result.succeed`/`Result.fail` against the installed package and corrected in a follow-up commit.
- **`scheduling/` — `Schedule` module is a near-total rewrite, verified against installed source**: `Schedule<Output, Input, Error, Env>` gained an `Error` type param (was 3 params, now 4). `Schedule.run`/`.delays`/`.driver`/`.union`/`.intersect`/`.andThen`/`.whileInput`/`.whileOutput`/`.tapInput`/`.tapOutput`/`.collectAllInputs` are all gone, replaced by `toStep`/`fromStep`/`min`/`max`/`concat`/`while`/`tap`/`upTo`. `Schedule.once`→`Schedule.duration(Duration.zero)`; `Effect.repeatN`→`Effect.repeat(effect,{times:n})`. `Cron.unsafeParse`→`Cron.parseUnsafe`; `Cron.parse` now returns `Result` not `Either`; `Schedule.cron`'s output changed from a `[start,end]` tuple to a plain `Duration.Duration`. `Schedule.jittered` dropped its min/max args (fixed 0.8–1.2 range now). A shared "log the schedule's delays" helper duplicated across ~15 fences had no direct replacement for `Schedule.run`/`.delays`/plain-callback `.addDelay` — rebuilt by manually driving `Schedule.toStep` + `Pull.matchEffect`.
- **`scheduling/` — table-vs-installed discrepancy**: `specs/_references/v3-to-v4.md` doesn't list `Schedule.andThen` in its Schedule rename table and it has no export in `Schedule.ts`; resolved via installed source — `Schedule.concat` has matching documented semantics and stayed a pipeable dual, no call-site restructuring needed.
- **`stream/` — extensive `Stream`/`Sink` restructuring, verified against installed source + runtime testing**: `Stream.runCollect` now returns a plain `Array`, not `Chunk` (pervasive; existing `// Output: {_id:'Chunk',...}` comments left untouched per spec). `Stream.scoped` now takes a `Stream`, not an `Effect` (only caught via a runtime `TypeError` during gating, not from static reading). `Stream.flatMap`'s callback must return a `Stream` — no longer implicitly lifts an `Effect`-returning callback (also only caught via runtime testing). `Stream.async`→`Stream.callback` (Queue-based push). `Stream.catchAll/catchAllCause/catchSome/catchSomeCause`→`catch/catchCause/catchFilter/catchCauseFilter` (`Filter`-based); `Stream.timeoutFail/timeoutFailCause/timeoutTo` collapsed into `Stream.timeoutOrElse`. `Stream.acquireRelease`/`Stream.finalizer` constructors removed → `Stream.scoped(Stream.fromEffect(Effect.acquireRelease(...)))`. `GroupBy` module removed entirely — `Stream.groupByKey`/`groupBy` now return plain `Stream<[K, Stream<V>]>`. `Stream.partitionEither`→`Stream.partitionEffect` with a **reversed** tuple order vs `partition`. `Stream.broadcast(n,lag)`→`Stream.broadcastN({n,capacity})` (bare `broadcast` repurposed for a different dynamic-subscriber API). `flatMap`'s `{switch:true}` option removed → dedicated `Stream.switchMap`. `Stream.as`/`concatAll`/`mapConcat`/`mergeWith` removed with documented compositional replacements.
- **`stream/` — removed API, no deterministic replacement**: `Stream.zipAll`/`zipAllWith` and `Schedule.compose` — reference guide only offers vague "hand-roll" suggestions, left untouched per no-speculation rule.
- **`stream/creating.mdx` — pedagogical content destroyed by the API change, human decision needed**: the "Unfolding vs. Pagination" section's entire contrast (`unfoldChunkEffect` vs `paginateChunkEffect`) no longer exists in v4 — both would notionally collapse onto the same `Stream.paginate`. All three large code blocks left completely untouched; this section needs a human rewrite, not a mechanical migration.
- **`error-management/` — extensive error-handling renames, verified by execution not just types**: `Random.nextRange`→`nextBetween`; `Cause.IllegalArgumentException`/`RuntimeException`/`isRuntimeException` fully removed→`Predicate.isError`/plain `Error`; `filterOrDie`/`filterOrDieMessage`/`tapErrorCause`→`tapCause`/`dieMessage`→`die`/`tapBoth` all reshaped; `Effect.either`→`result`; `Effect.cause` reshaped around `Effect.exit`; `Effect.merge`→`catch`; `Effect.orElse`→`catch`, `orElseFail`→`mapError`; `Effect.orDieWith`→`mapError`+`orDie` (pipe form doesn't type-check, needed direct calls); `Effect.catchAllDefect`→`catchDefect`; `TimeoutException`→`TimeoutError`; `timeoutFail`/`timeoutFailCause`/`timeoutTo` all consolidated into `timeoutOrElse`; `Effect.disconnect` removed, rebuilt with `forkDetach`+`Fiber.join` (verified end-to-end with a real ~6s run); `Cause.isDieType`→`isDieReason`; `Cause`'s `_tag`-switch pattern (`matchCause`, `catchTags` on a Cause) rewritten to a `for (reason of cause.reasons) switch (reason._tag)` loop, since Cause no longer has a single top-level `_tag` (consistent with the `{failures/reasons:[...]}` array-of-reasons shape confirmed independently in `getting-started/running-effects.mdx` and `concurrency/`).
- **`error-management/` — removed API, no replacement**: `Effect.validateFirst`, `Effect.parallelErrors`, `Effect.catchSomeDefect` — all confirmed absent from installed source, left unchanged.
- **`error-management/expected-errors.mdx` — behavior change breaking the file's own teaching point**: `Effect.catchSome`→`Effect.catchFilter` now narrows the type (v3's `catchSome` didn't), which changes what the section demonstrates. Flagged, left as the closest faithful migration (no alternative exists).
- **`error-management/parallel-and-sequential-errors.mdx` — behavior change, verified by repeated execution**: v4's concurrent `Effect.all`/`Effect.ensuring` no longer combine simultaneous errors into a multi-reason `Cause` for this file's synchronous demos — confirmed empirically, not inferred. `Effect.parallelErrors`'s suggested `Effect.all(mode:"result")` reconstruction (per the migration guide) doesn't actually work either — verified a concurrent defect still short-circuits before results can be collected, so that reference-doc suggestion is itself wrong.
- **Reference doc discrepancy**: `migration/MIGRATION.md` does not exist at pinned commit `a94cbed84e9e49bea4bff925599c0f19c4e3deab` (confirmed via GitHub contents API — the `migration/` dir at that commit contains only the individual guide files listed in spec §4.2 plus an `annotations/` subdirectory of per-module YAML data not referenced by the spec). All other listed guide files (`v3-to-v4.md`, `services.md`, `error-handling.md`, `forking.md`, `yieldable.md`, `fiberref.md`, `runtime.md`, `scope.md`, `equality.md`, `cause.md`, `layer-memoization.md`, `fiber-keep-alive.md`, `generators.md`, `schema.md`) fetched successfully and cached under `specs/_references/`.

## Verification log

### G0 — Setup

- Vendored `@effect/doctest` from `Effect-TS/effect` at commit `c1ed0ac97cd04e8ed359ca7956ba604a88fc2bb6` (`packages/tools/doctest/{package.json,src/**}`) into `vendor/doctest/`. Version matches installed `effect@4.0.0-beta.104`.
- Applied `.mdx` format-detection patch (`patches/doctest-mdx.patch`) to `vendor/doctest/src/Source.ts`.
- Added devDependencies: `@effect/doctest@file:vendor/doctest`, `vitest@^4.1.10` (resolved `4.1.10`), `rolldown@^1.1.5` (resolved `1.2.3`), `@types/node` (resolved `26.1.2`).
- `pnpm peers check` reports an unmet `effect` peer for `@effect/doctest` — expected: the vendored `package.json` still declares `"effect": "workspace:^"` from its source monorepo; harmless outside that workspace. Pre-existing unrelated peer warnings for `astro`/`typescript` (astro-tweet, astro-expressive-code, i18next, @astrojs/check) were present before this change.
- Created `vitest.docs.ts`, added `doctest` / `doctest:file` scripts to `package.json`.
- Created `scripts/extract-doctest-snippets.mjs` (type-gate extraction for G2).
- Added `test-results/` to `.gitignore`.
- **F1 workaround needed**: Node's native TypeScript type-stripping refuses to process `.ts` files located under `node_modules` (`ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`), which vitest's config loader and plain `node` both hit when importing `@effect/doctest/Plugin` / `@effect/doctest/Source` via the package specifier (resolves into the pnpm store, physically under `node_modules`). Fix: `vitest.docs.ts` and `scripts/extract-doctest-snippets.mjs` import directly from the vendor path (`./vendor/doctest/src/Plugin.ts`, `../vendor/doctest/src/Source.ts`) instead of the package specifier — same source, but outside `node_modules` so Node's native loader strips types normally. This is a project-local workaround (not a change to the vendored package itself) and is distinct from the spec's anticipated `.mdx`-passthrough-module workaround, which was not needed.
- **Smoke test**: `src/content/docs/v4/_smoke.mdx` with one Class-A fence (`const sum = 1 + 1 // => 2`) — `pnpm doctest` discovered and passed 1/1 test. File deleted after verification, per §8.1.7.

### Post-G0 infra fixes (found while migrating `batching.mdx`)

- **Doctest snippet modules weren't parsed as TypeScript.** Extracted snippet virtual module ids keep the source file's `.mdx` extension (e.g. `batching.mdx?effect-doctest=snippet&index=5`), so Vite's extension-based loader selection skipped its TS transform and raw `interface`/type-annotation syntax reached the plain-JS parser, failing all 8 marked fences with a parse error. Fixed with a `pre` Vite plugin in `vitest.docs.ts` that strips types on ids matching the doctest snippet protocol before Vite's default transform runs, using `node:module`'s `stripTypeScriptTypes` (not the `typescript` package's `transpileModule` — this repo's `typescript` dependency is TS 7 / tsgo, whose npm package no longer ships the classic compiler API).
- **`pnpm check` (root `tsc -b`) pulled `vendor/doctest` into the app's strict type-check.** The root tsconfig's `"**/*"` include plus `allowJs: true` (from `astro/tsconfigs/strictest`) reached `vendor/doctest/src/*.ts` via `scripts/extract-doctest-snippets.mjs`'s and `vitest.docs.ts`'s relative imports, failing on `noUncheckedIndexedAccess`/`exactOptionalPropertyTypes` errors in vendored code never written against those settings. Fixed by excluding `vendor`, `test-results`, `vitest.docs.ts`, and `scripts/extract-doctest-snippets.mjs` from the root `tsconfig.json` — the latter two are tooling entry points, not app source, and are type-checked implicitly when run (or by `tsconfig.doctest.json`) rather than by `tsc -b tsconfig.json`.
- **G2 type gate config.** Added `tsconfig.doctest.json` (extends root, `moduleResolution: bundler`, `types: ["node"]`) with `noUnusedLocals`/`noUnusedParameters` disabled: narrative doc fences intentionally leave some declarations unused within a single isolated extracted snippet (e.g. a resolver shown only for contrast against a later approach, or a model interface reused by a *different* fence in the same file but not the current one) — this isn't a type-correctness problem, just an artifact of type-checking each fence as a standalone file.

### `batching.mdx` (G1–G3)

- G1 (`pnpm doctest:file`): 8/8 passed.
- G2 (`tsc -b tsconfig.doctest.json` over the 8 extracted snippets): clean after 3 real fixes (not just renames) — see the migration commit body for the full list: `Effect.andThen` doesn't accept a callback returning a plain (non-`Effect`) value in v4, so the "resolve `HttpService` before constructing the resolver" pattern needed `Effect.map`, not `Effect.andThen`; `Request.tagged` constructors for tag-only requests (no extra fields) are called with no arguments in v4 (`GetTodos()`, not `GetTodos({})`) since `Request.Constructor` now applies `Types.VoidIfEmpty`; and one `RequestResolver.fromEffect` callback needed an explicit return-type annotation for its success type to infer correctly through the `Effect.map` wrapper (matches the annotation style already used elsewhere in the same file).
- G3 (`pnpm check`, `pnpm fmt`): clean. `oxlint` has no `.mdx` coverage in this repo, so lint is not applicable to doc fences.

### Post-`batching.mdx` infra fix (found while migrating `configuration.mdx`)

- **`node:module`'s `stripTypeScriptTypes` with `mode: "strip"` rejects constructor parameter properties.** `configuration.mdx`'s demo classes (`HostPort`, `ServiceConfig`) use `constructor(readonly host: string, ...) {}`, which "strip" mode can't handle (removing the modifier requires *generating* an assignment, not just deleting type syntax) — failed 5 of 9 marked fences with "TypeScript parameter property is not supported in strip-only mode". Fixed by switching `vitest.docs.ts`'s plugin to `mode: "transform"`. Re-verified `batching.mdx` still 8/8 after the change.

### `configuration.mdx` (G1–G3)

- G1 (`pnpm doctest:file`): 9/9 passed (after the transform-mode fix above and the `ConfigProvider.orElse` runtime-bug fix noted under Unresolved items).
- G2 (`tsc -b tsconfig.doctest.json` over 17 extracted snippets total across both migrated files so far): clean.
- G3 (`pnpm check`, `pnpm fmt`): clean.
- Config/ConfigProvider turned out to be one of the more heavily restructured modules encountered so far — see the migration commit body for the full list of reshaped APIs (`Config.schema` replacing `Schema.Config` with swapped argument order, `Config.Array`/`Config.Record` as Schema producers, `ConfigProvider.fromUnknown` replacing `fromJson`, `Effect.provideService(effect, ConfigProvider.ConfigProvider, provider)` replacing the removed `Effect.withConfigProvider`, etc.). All of it was verified directly against `node_modules/effect/src/{Config,ConfigProvider,Schema}.ts`, including reading the JSDoc usage examples embedded in that source (which are themselves doctested in the `effect` package) as ground truth for exact call shapes.

### `runtime.mdx` (G1–G3)

- G1 (`pnpm doctest:file`): 6/6 passed.
- G2 (`tsc -b tsconfig.doctest.json` over 23 extracted snippets total across all three root files): clean after one real fix — the "Using ManagedRuntime in an External Framework" fence had a second, easy-to-miss `Notifications.notify(...)` static-proxy call inside `main()`'s body (separate from the two proxy-access fences found during initial migration), only caught because the type gate flagged `Property 'notify' does not exist on type 'typeof Notifications'` on the extracted snippet. A grep afterward for the same pattern across the file confirmed no other occurrences remained.
- G3 (`pnpm check`, `pnpm fmt`): clean.

### `getting-started/building-pipelines.mdx` (G1–G3)

- G1 (`pnpm doctest:file`): 11/11 passed.
- G2 (`tsc -b tsconfig.doctest.json` over 34 extracted snippets total): clean (2 pre-existing, non-migration-related `effect(globalErrorInEffectFailure)` style warnings from the language-service plugin on `Effect.fail(new Error(...))` calls that predate this migration — warnings, not errors, don't fail the gate).
- G3 (`pnpm check`, `pnpm fmt`): clean.
- First file in the `getting-started` directory (sidebar order: getting-started, error-management, requirements-management, resource-management, observability, scheduling, state-management, caching, concurrency, stream, sink, testing, code-style, data-types, trait, behaviour, schema, ai, platform), processed alphabetically within the directory per §9.

### `getting-started/control-flow.mdx` (G1–G3)

- G1 (`pnpm doctest:file`): 19/19 passed.
- G2 (`tsc -b tsconfig.doctest.json` over 53 extracted snippets total): clean.
- G3 (`pnpm check`, `pnpm fmt`): clean.
- Deepest API surface change encountered so far: 5 removed `Effect` combinators (`if`, `loop`, `iterate`, `unless`, `unlessEffect`), one consolidated/restructured combinator (`when`/`whenEffect` → single `when`), and a reshaped `Effect.all` `mode` option (`"either"` → `"result"`, `"validate"` dropped entirely). All confirmed directly against `node_modules/effect/src/Effect.ts` and cross-checked with `specs/_references/v3-to-v4.md`'s removal table.

### `getting-started/creating-effects.mdx` (G1–G3)

- G1 (`pnpm doctest:file`): 17/17 passed.
- G2 (`tsc -b tsconfig.doctest.json` over 70 extracted snippets total): clean (pre-existing untagged-`Error` style warnings only).
- G3 (`pnpm check`, `pnpm fmt`): clean (fmt auto-fixed cheatsheet table column widths after the `async`→`callback` rename lengthened one cell).
- Lighter file than the previous two: mostly straightforward renames (`Effect.async`→`Effect.callback`, `Effect.fork`→`Effect.forkChild`, `UnknownException`→`UnknownError`), no removed-with-no-replacement APIs.

### `getting-started/devtools.mdx` (G1–G3)

- G1: 0 marked fences — vacuously passes. Almost the entire file is prose/shell/JSON (installation instructions, feature lists), out of migration scope.
- G2: no new snippets contributed (unchanged extraction count).
- G3 (`pnpm check`, `pnpm fmt`): clean.

### `getting-started/{importing-effect,installation,introduction,running-effects,the-effect-type,using-generators,why-effect}.mdx` (G1–G3)

All green (`pnpm doctest:file`, `tsc -b tsconfig.doctest.json`, `pnpm check`, `oxfmt --check`) for every file. `importing-effect.mdx` and `introduction.mdx` needed no changes at all (no commit, per protocol). This closes out `getting-started/` — all 11 files done.

### Multi-agent parallel pass (all other directories)

At the user's explicit request, the remaining 18 directories (error-management, requirements-management, resource-management, observability, scheduling, state-management, caching, concurrency, stream, sink, testing, code-style, data-types, trait, behaviour, schema, ai, platform) were migrated by 18 concurrent background agents, one per directory, each following this same spec and gate protocol, each restricted to its own directory's files via pathspec-scoped commits. This is a deviation from the spec's literal "never parallelize two directories" instruction — noted here as an explicit, requested deviation, not a silent one. Each agent's own findings are being consolidated into this report as they complete; see the per-file table and per-directory verification entries below for each.

A shared infra hiccup during the parallel run: concurrent `pnpm install`-triggered `effect-tsgo patch` prepare hooks accumulated over 100 backup copies of the local `tsc` binary, hitting a hard-coded safety limit and blocking every agent's `pnpm` commands ("Too many backup files exist"). Fixed by deleting the redundant numbered backups (a `node_modules` cache artifact, not tracked work) after explicit user confirmation, per the tool's own suggested remedy.

### `testing/`, `state-management/`, `caching/`, `behaviour/`, `trait/`, `sink/`, `code-style/`, `resource-management/`, `requirements-management/`, `observability/`, `ai/`, `platform/` (G1–G3, all agents complete)

All 12 directories' agents reported every gate green for every file they committed (`pnpm doctest:file` / `npx vitest run --config vitest.docs.ts`, the corpus-wide `tsc -b tsconfig.doctest.json` type gate, `pnpm check`, `oxfmt --check`). Several agents hit the shared tsc-backup infra issue transiently and worked around it by invoking `npx vitest`/`npx tsc` directly instead of the `pnpm` script wrappers (same underlying commands). No agent reported a file it couldn't get green — see the per-file table and Unresolved items above for the substantial API-shape findings each surfaced. `code-style/dual.mdx` and `code-style/guidelines.mdx` needed no changes; `ai/introduction.mdx` needed no changes; `observability/supervisor.mdx` and `sink/concurrency.mdx` were left entirely unchanged (removed API, no replacement, whole file's content is built around it).

Remaining directories still in progress: error-management, scheduling, concurrency, stream, data-types, schema.

### G4–G5

Not yet run — remaining directories (error-management, scheduling, concurrency, stream, data-types, schema) still in progress; final consolidation and sweep pending until all parallel agents complete.
