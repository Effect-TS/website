import { Card, Cards } from 'nextra-theme-docs'
import meta from "./core-concepts/_meta.json"

export const prefix = "core-concepts"

# Core Concepts

The following concepts will help you develop a basic understanding for how to work with Effect.

<Cards>
  {Object.entries(meta).map(([key, page]) => (
    <Card key={key} title={page} href={`/docs/${prefix}/${key}`} />
  ))}
</Cards>
