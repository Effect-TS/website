import meta from "./tutorials/_meta.json"

export const prefix = "tutorials"

# Tutorials

{Object.entries(meta).map(([group, page]) => {
const meta = require(`./tutorials/${group}/_meta.json`)
return (

<div>
  <h2>{page}</h2>
  <ul>
    {Object.entries(meta).map(([key, tutorial]) => (
      <li>
        <a href={`./tutorials/${group}/${key}`}>{tutorial}</a>
      </li>
    ))}
  </ul>
</div>
) })}
