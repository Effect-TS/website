import meta from "./tutorials/_meta.json"

# Tutorials

{Object.entries(meta).map(([group, page]) => {
const meta = require(`./tutorials/${group}/_meta.json`)
const entries = Object.entries(meta)
if (entries.length === 0) {
return (

<div>
  <h2>{page}</h2>
  <ul>
    <li>
      <a href={`./tutorials/${group}`}>{page}</a>
    </li>
  </ul>
</div>

)
}
return (

<div>
  <h2>{page}</h2>
  <ul>
    {entries.map(([key, item]) => (
      <li>
        <a href={`./tutorials/${group}/${key}`}>{item}</a>
      </li>
    ))}
  </ul>
</div>
) })}
