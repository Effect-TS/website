---
import DitheredOverlay from "@/components/DitheredOverlay.astro"
import Footer from "@/components/Footer.astro"
import GridRails from "@/components/GridRails.astro"
import Navigation from "@/components/navigation/Navigation.astro"
import BaseLayout from "@/layouts/BaseLayout.astro"
import Main from "@/components/layout/Main.astro"
import type { NavigationActiveSlug } from "@/lib/navigation"

interface Props {
  readonly title?: string | undefined
  readonly description?: string | undefined
  readonly activeSlug?: NavigationActiveSlug | undefined
  readonly themeable?: boolean | undefined
}

const { title, description, activeSlug, themeable = false } = Astro.props
---

<BaseLayout
  {...title ? { title } : undefined}
  {...description ? { description } : undefined}
  {...!themeable ? { forceDark: true } : undefined}
>
  <div class="relative min-h-screen bg-background text-foreground antialiased">
    <DitheredOverlay />
    <GridRails />

    <Navigation {...activeSlug ? { activeSlug } : undefined} />

    <Main class="relative w-full">
      <slot />
    </Main>

    <Footer />
  </div>
</BaseLayout>
