---
import { Code } from "astro-expressive-code/components"

import type { ApiCodeExample } from "./ApiReference"

interface Props {
  example: ApiCodeExample
}

const { example } = Astro.props
---

<article>
  <div class="mb-2 flex items-baseline gap-1.5">
    <h3 class="text-base font-semibold text-foreground">Example</h3>
    {example.title && <p class="text-sm text-muted-foreground">({example.title})</p>}
  </div>

  <Code code={example.source} lang={example.language} meta="showLineNumbers=false" />
</article>
