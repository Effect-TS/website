import { Card, Cards } from 'nextra-theme-docs'
import meta from "./advanced-concepts/_meta.json"

export const prefix = "advanced-concepts"

# Advanced Concepts

The following topics are considered advanced and will allow you to dive deeply into the internals of Effect.

<Cards>
  {Object.entries(meta).map(([key, page]) => (
    <Card title={page} href={`/docs/${prefix}/${key}`} />
  ))}
</Cards>
