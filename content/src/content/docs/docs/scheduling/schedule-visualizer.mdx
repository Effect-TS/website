---
title: Schedule Visualizer
---

import { ScheduleVisualizer } from "@/components/schedule-visualizer/ScheduleVisualizer"
import { Duration } from "effect"

# Schedule Visualizer

<ScheduleVisualizer
	code={`Schedule.fixed(Duration.seconds(1))`}
  items={[
    { value: "0", interval: Duration.seconds(1), clock: Duration.seconds(0), hasNewValue: true },
    { value: "1", interval: Duration.seconds(1), clock: Duration.seconds(1), hasNewValue: true },
    { value: "2", interval: Duration.seconds(1), clock: Duration.seconds(2), hasNewValue: true },
    { value: "3", interval: Duration.seconds(1), clock: Duration.seconds(3), hasNewValue: true },
  ]}
/>

