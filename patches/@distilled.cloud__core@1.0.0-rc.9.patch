--- a/lib/retry.js
+++ b/lib/retry.js
@@ -72,7 +72,7 @@
  * Layer that pins the server `retryAfter` cap to a fixed value (milliseconds).
  */
 export const serverRetryHintCapLayer = (capMs) => Layer.succeed(ServerRetryHintCapMs, capMs);
-const serverRetryHintCapMsConfig = Config.string(ENV_SERVER_RETRY_HINT_CAP_MS).pipe(Config.map((raw) => {
+const serverRetryHintCapMsConfig = Config.String(ENV_SERVER_RETRY_HINT_CAP_MS).pipe(Config.map((raw) => {
     if (raw === "")
         return DEFAULT_SERVER_RETRY_HINT_CAP_MS;
     const n = Number(raw);
--- a/src/retry.ts
+++ b/src/retry.ts
@@ -121,7 +121,7 @@
 export const serverRetryHintCapLayer = (capMs: number) =>
   Layer.succeed(ServerRetryHintCapMs, capMs);
 
-const serverRetryHintCapMsConfig: Config.Config<number> = Config.string(
+const serverRetryHintCapMsConfig: Config.Config<number> = Config.String(
   ENV_SERVER_RETRY_HINT_CAP_MS,
 ).pipe(
   Config.map((raw) => {
